# dependencies / build
node_modules/
dist/

# cloudflare / wrangler
.wrangler/
.dev.vars

# os / editor
.DS_Store
*.log

# portal (local workbench — secrets + runtime state)
portal/.env
portal/.sessions.json
