# Sweep — reports slice 3 (files 77–113 of `.claude/reports/*.md`, sorted)

| file | ticket/PR | codes | evidence | PARTIAL? |
|---|---|---|---|---|
| `public-round-trip-demo-factory-stage-report.md` | #42 | P5, P6, N | P5: "shared working dir held uncommitted parallel-session (motion) work". P6: muted-not-strikethrough "plan's wording licenses this"; notProposed accordion "plan flagged it optional"; Station-02 link "plan's sanctioned alternative". N: base moved 5b2a768→1fb8833 mid-session, anchors re-derived. | no |
| `recorder-hardening-report.md` | #25 | P1, P6, P7, N | P1: plan's VALIDATE one-liner "expects `trace: empty`… actual error is `trace line 1: not JSON`". P6: prompt tightened — "plan's own documented edge case with its prescribed remedy". P7: snippet wrote `verdict.message` raw vs AC #1 "every string"; AC #1 live proof "the plan's integration list didn't otherwise provide". N: cleanup `pkill` "briefly killed that pre-existing instance" of the portal. | no |
| `scenario-packages-worker-mock-api-report.md` | #4 | P1, P5, P8, P10 | P1: `compatibility_date` "today" rejected by wrangler, pinned to 2026-05-03. P5: tree held #3's outputs — "commit for this ticket must stage selectively". P8: plan's `rewardType` `none` "doesn't exist in the engine". P10: `cache: "no-store"`, coherence proofs "beyond plan", `stage` field added. | no |
| `shared-link-brand-restore-report.md` | #108 | P1, P3, P5, P7, N | P1: plan predicted 11,600→11,700; "went to 11,800". P3: plan/repo note said "below pixelmatch's per-pixel threshold" — actual mechanism `maxDiffPixels: 100`. P5: "parallel session added an untracked `.claude/plans/ds-list-row-primitive.md`". P7: "plan's 8 rows… none exercised the arrival-notice clause, so AC #8 had no automated evidence". N: #77 report's table "mislabels the control". | no |
| `site-shell-ia-analytics-report.md` | #6 | P1, P5, P6, P7, P8, N | P1: Deck H wrote `/derive.html`; route is `/derive`. P5: shared dir "on a parallel session's WIP branch". P6: scenarios row "DROPPED (plan-sanctioned)"; §05 wiring "Out of Scope". P7: GOTCHA wants h1→h2→h3 but Decks E/F give a div; CTA needs `#layers` "the plan assigned §02 no id". P8: linked pages "not on main (they're on open PR #16)". N: checklist amendment line deferred post-merge. | no |
| `spike-1-verdict.md` | #203 | P1, P10, P13 | P1: `--dry` "~$0.447 — roughly double the plan's $0.10–0.25 estimate". P10: fence and projector parsers merged into one `parseOpCommand`. P13: "Do not explore or orient first" — memory `recorder-run-positive-framing` "already records" it. | no |
| `spring-motion-foundation-165-report.md` | #165 | P1, P5, P6, P11, P12, P13 | P1: "plan said remove the whole `:1854-1858` block" — second rule not redundant; `.card` check subject wrong page. P5: main dir "sits on `feature/param-count-167` (a parallel session's branch)". P6: restore-row entrance "Open Question 3 called it optional". P11: Level 4 "no OS-level manual pass performed". P12: CI caught stale `system-graph.json` — "plan's Level-1 validation only watched tokens + handoff". P13: components.css→system-graph cascade already in #80 report; worktree `npm ci` "known from repo memory". | no |
| `st-ux-wave1-strategy-copy-244-report.md` | #244 (PR #246) | P6, P7, P9, N | P6: six copy edits — "each is a constraint the plan itself stated"; split "pre-authorized" by Task-4 GOTCHA. P7: draft said "illustrations" while "the plan's own AC requires the word *illustrative*". P9: "Task 5 ran on one of its three named targets… the reduction went unrecorded" until review L1. N: Task 11 "post-merge, not yet done". | no |
| `studio-249-inp-gate-review-fixes-report.md` | #249 (PR #247 follow-up) | — | "None in substance"; settle-timeout flake re-run per memory; ports 4757/4759 occupied, ran on 4761. | no |
| `studio-adoptboard-cancel-264-report.md` | #264 | N | N: main advanced (#274–#276) mid-flight; evidence split pre-/post-rebase, loc regen zero drift. | no |
| `studio-and-board-ops-quartet-225-231-232-236-report.md` | #225/#231/#232/#236 | P4, P8, P12 | P4: "Two of those mutations also caught weak checks in the drafts" — teardown case re-proved case 1; L3 case "passed with and without the fix". P8: envelope check in `checkOp` ("the obvious reading of the issue") turned build-checks red; belongs at `parseOpCommand`. P12: loc 23,000→23,200 renders on approach, baselines "deliberately NOT re-taken". | no |
| `studio-canvas-affordances-217-report.md` | #217 (PR #263) | P4, P7, P8, P10, P11, P13, N | P4: R5 assertion "vacuously true both ways: a check that cannot fail"; R7 "would have passed for the wrong reason". P7: rename vs "task's own R3 GOTCHA"; Task 6 press-target vs D2's sentence; illustrative string vs `SPOKEN_MAX`. P8: D11 both-live state "does not exist to test"; column 12 pointer-unreachable. P10: `data-stx-verb` attrs, fourth-family check "the plan did not ask for". P11: "real-Safari / real-Chrome manual pass… was NOT run". P13: `selectPass` scroll-into-view parking (smooth-scroll trap in #108/#214). N: pre-existing Escape/dock defect "Worth its own ticket"; review's suggested fix "does not work". | no (manual pass outstanding) |
| `studio-canvas-manipulation-205-report.md` | #205 | P1, P4, P6, P7, P9, P10 | P1: plan's 1.5× zoom case "would fail for a reason unrelated" — 0.75 used. P4: `deep()` replacer bug — "Every deep-compare in the group was vacuous"; `clampSlot` and `limit` unreachable. P6: R3 downgrade "plan pre-authorised", not taken. P7: `aria-pressed` — "mount task said to set it; its `place()` task and its Level 4 pass both say not to". P9: "plan specified 'inside the wrapper' and I had dropped the containment half". P10: `grid-template-rows`, `SPOKEN_MAX` cap "Not in the plan". | no |
| `studio-canvas-stage-204-report.md` | #204 | P1, P4, P6, P13 | P1: sketch `calc()` omitted grid gap, "fell ~176 px short at 12 columns". P4: 16.7 ms criterion "cannot distinguish our work from nothing at all" — idle scores same. P6: A1/A2/A3/A5 "confirmed as written" listed as deviations. P13: "The first run rewrote nothing" — sub-perceptual skip already in #108 report. | no |
| `studio-compile-beat-207-report.md` | #207 (PR #235) | P1, P8, N | P1: "Announcement count is 5, not 4". P8: `OUT_OF_LIBRARY` "false on a canvas with no downloads"; readout beside buttons — "Measured on the running page, that does not work". N: base reported as #206's branch (squash-merged); `.stx-viewport` overflow "worth a follow-up ticket". | no |
| `studio-compile-identity-tripwire-253-report.md` | #253 | P1, P5, N | P1: loc rounding "DID flip (the plan called it 'almost never')". P5: "parallel #215 session switched the shared checkout" and committed mid-gates; first `update:docker` "captured the #215 tree". N: block `p6` not plan's `p4` — PR #254 merged after plan. | no |
| `studio-compiled-screens-overflow-251-report.md` | #251 | P1, P11, N | P1: "Line anchors shifted vs the plan… studio.css:451 not :434, `mountCompile` at :456 not :424". P11: "real-Safari/Chrome-stable eyeball… remains for the owner". N: branched off #214 tip, PR stacked on #252. | no |
| `studio-epic-close-223-report.md` | #223 | P2, P6, P8, P10, P13, N | P2: "PARTIAL… four remaining items are operator/owner-gated" — deploy OK, live run, testers, Phase E close. P6: export-fails case "the plan left it optional". P8: `gen-pack-bundle.mjs` — "the plan's list omitted it". P10: extra `data-provenance="visitor"` assertion "beyond the plan's four steps". P13: worktree `npm ci` "known from memory". N: `pkill -f serve.mjs` "may have stopped a parallel session's server". | yes |
| `studio-export-keep-rail-210-spike3.md` | #210 Task 1 | P1, P8 | P1: plan's `/@import[^;]*;/g` "deletes the entire saulera pack"; `grid-auto-flow` snippet "does *not* carry" the arrangement. P8: `cloneNode` fallback banned by build-checks `.innerHTML` rule; drafted font sentence assumed a face "that does not exist on the live site either". | no |
| `studio-flows-places-screens-212.md` | #212 | P6, P7, P8, P12, P13, N | P6: `components.css` untouched "per the plan's own recorded decision". P7: `compose()` — "the plan named both call sites". P8: plan said studio-canvas "need zero changes" — pan swallowed nav clicks; group 6/journey rows "asserted the pre-#212 DOM". P12: first loc regen "under-counted by 200; caught by `--check` on the committed tree", baselines re-captured. P13: `loc-summary-counts-tracked-only` trap already in #206 report/memory. N: local main "carried two stale near-duplicate fix(210) commits", reset. | no |
| `studio-gates-213-report.md` | #213 | P5, P6, P8, N | P5: shared dir "picked up #212's in-progress uncommitted edits", moved to `wt-213`. P6: red-run method — "the plan offered that or the self-test". P8: flagged row "cannot be replayed in isolation" (order-dependent); plan's order "would measure a disabled button" (#240). N: ticket bullets "already satisfied incrementally" by earlier tickets; saulera `fonts.css` 404 console exemption. | no |
| `studio-ia-re-point-home-gate-216-report.md` | #216 | P1, P3, P7, P8, N | P1: "`npm run test:docker` does not exist"; orphaned classes "nine… not one". P3: plan said routes "stay in the footer index" — false; "the comment that repeated it has been corrected". P7: `instance.mjs` scoped out vs completion checklist's "no-stale-comment rule". P8: "11 dangling comment citations across 7 files", `portfolio.css:914`, meta description — none in plan. N: Task 23 post-merge; pre-existing dead `/handoff` link flagged. | no |
| `studio-inspector-docs-218-report.md` | #218 | P3, P4, P8, P10, P13, N | P3: catalog.mjs prose "since #215… this plan repeated it" — `[hidden]` load-bearing claim false. P4: row 6 mutation "STAYED GREEN"; "no gate can catch its removal today". P8: "The plan did not foresee that two of the three DOCS_SOURCES have other consumers"; chip string overflowed rail. P10: `z-index` hit-test fix on pre-existing `studio.css` defect. P13: "Two driver traps, both already recorded elsewhere in this repo". N: group 23 not 22 — #217 landed after plan. | no |
| `studio-instance-reshell-222-report.md` | #222 (PR #270) | P1, P4, P5, P8, N | P1: plan's grep target "read as 'no live references'" — two historical mentions remain. P4: "without it the plan's own pack.json mutation drill could never go red"; M2 made it red "deterministically". P5: "shared worktree held four parallel-session staged files". P8: peak receipts machinery "the plan did not name"; `parseCompanyBrief` pins slug to directory; `[data-studio-notice]` exemption. N: saulera `@import`s unshipped `fonts.css` — "pre-existing saulera quirk". | no |
| `studio-layers-minimap-221.md` | #221 (PR #272) | P6, P7, P8, P13, N | P6: empty-state "plan itself marked this 'a journey micro-case if cheap' — it was not cheap". P7: Task 4 rebuild-on-any-mutation contradicted "the plan's own NOTES discipline". P8: "the plan missed a recorded /factory constraint"; "viewBox constant" claim hid staleness. P13: constraint "already recorded in studio-journey's R5 note"; null-destructure from `studio-select.mjs:110-113`; centre-parking "methodPass's rule". N: groups 26/27 not 25/26 — #222 merged after plan. | no |
| `studio-method-cards-hook-loop-214-report.md` | #214 (PR #252) | P8, P10, P13 | P8: plan's "not touching replay-driver.mjs" — seek slider "REBUILDS the run's board" over a redraft; two provenance values, `?b=` needed a third. P10: canvas-column track fix "(not in the plan)". P13: that fix is the "documented-but-deferred layout phenomenon" (#207); smooth-scroll races "memory… re-confirmed". | no |
| `studio-protos-as-frames-219.md` | #219 (PR #267) | P1, P6, P8, P10, N | P1: `npm run test:docker` — "The plan names it in three places". P6: footprints — "The plan asked for this decision to be made in a real browser". P8: "The plan's Task 11 branch table was not exhaustive" — six more sites; `hitSlot` promised, unnecessary; #217 fixtures assumed empty cells. P10: `anchor` scroll, `EXPECTED_NOISE`/`mainOnly`, two group-24 checks "Not in the plan". N: review M2/M3 corrected two false claims in first wording. | no |
| `studio-replay-driver-takeover-209-report.md` | #209 | P1, P4, P6, P7, P8, P9, P12, P13 | P1: plan "quoted 10.9×"; measured 9.3×. P4: "Pause is hidden" assertion "vacuous"; pause detector "milliseconds wide"; no-op `addInitScript` under a coverage comment. P6: CSS in studio.css "as the plan recorded". P7: missing `fromStep` — "The plan said both in different places". P8: `place()` "idempotent but not inert"; import cycle; race in "THREE places, not one"; Playwright 30 s default unbudgeted. P9: "Two plan-named edge cases had no check" — caught by review. P12: loc regen "measured a tree that then kept growing — caught by drift-check only after committing". P13: "the second two were the two recorded traps". | no |
| `studio-replay-recorder-203-report.md` | #203 (PR #224) | P1, P5, P8, P10, P13, N | P1: `--dry` "~$0.447, roughly double the plan's $0.10–0.25 estimate"; Task 7 sketch "resolved a `rawPath` it never used". P5: "two untracked plan files belonging to other tickets (#173, #176)". P8: self-loop refusal mirrors `breadboard.mjs:352` — "Not in the plan's Task 1 list". P10: two parsers → one shared `parseOpCommand`. P13: prohibition prompt — memory `recorder-run-positive-framing`. N: PR #201 merged mid-ticket, PR `CONFLICTING`, resolved by regeneration. | no |
| `studio-route-surgery-orchestrator-206-report.md` | #206 | P1, P7, P8, P10, P12, P13 | P1: "The plan's shell-geometry note is slightly wrong" — three places fit, none off-stage. P7: `buildSummary` shape omitted `reason` while "SURFACE SPEC requires the pattern's `reason` verbatim". P8: inner controls "already in `portfolio.css`", so no `bx-*` port. P10: `<noscript>` added. P12: loc-summary "regenerated at its planned point… then went stale… Caught by the post-commit sweep" — extra commit and regen. P13: "exactly the `loc-summary-counts-tracked-only` trap". | no |
| `studio-share-codec-v2-208-report.md` | #208 | P1, P4, P6, P12, N | P1: "A bare array cannot carry" `$description`; sketch's message "a lie about *why*". P4: bomb case "would have left that case green for the wrong reason… 'check that cannot fail' shape, introduced by this ticket". P6: Tasks 7/8 kept "per the plan's own Open Question 2". P12: approach digit 23,200→23,300 stale in baseline; "this PR carries no baseline change" by choice. N: group ordinals — file "has since grown groups 14… and 15". | no |
| `studio-single-file-export-keep-rail-210-report.md` | #210 | P1, P4, P7, P8, P10, N | P1: plan's `@import` regex — "the whole saulera pack drops out". P4: group 17 substring test — "restoring the plan's regex left it green"; ampersand mutation "left every other assertion green". P7: Task 7 self-booting tag "would race the `?b=` branch Task 9 introduces". P8: `TWO_CLAIMS` alone dishonest at rest; "the same sentence `renderUnavailable` already prints" was a local literal; surplus slots reachable. P10: `[data-studio-notice]` node added. N: `build-journey` webkit flake "PRE-EXISTING", own ticket. | no |
| `trace-recorder-player-report.md` | #5 | P1, P5, P6, P7, P8, P10 | P1: plan's `allowedTools` list "would have **bypassed the write-fence**". P5: "other sessions actively modified `CLAUDE.md`, the six existing specs…"; `pack.json` entanglement. P6: `claude-sonnet-5` "within plan assumption #3". P7: literal start-anchored regex vs plan prose "marker alone on its own line". P8: `--dry` fence root needs `realpathSync` — "not in the plan". P10: display-time path relativization "advisor-flagged polish". | no |
| `v3-approach-tightened-work-proof-index-report.md` | #80 (PR #95) | P1, P8, P11, P12, N | P1: plan assumed Approach's LoC "unaffected" — dropped 9,400→9,300. P8: "keep the two live-demo cards verbatim" — claim "false after #78". P11: "Not done… a real Safari + Chrome eyeball (CHECKLIST MUST)" and humanizer pass. P12: `system-graph.json` regen "unanticipated cascade" caught by blocking drift-check. N: origin/main "advanced 11 commits"; merged, regenerated. | no |
| `v3-built-screen-peak-report.md` | #75 | P7, P8, P12, P13 | P7: Task 5 "color-* on the stage" vs Task 13 + Gate #2 — "a plan-internal contradiction"; Task 10 analytics slot vs AC. P8: `stat-tile.kind` enum-locked cannot express the still's copy; "factory.html is a documented stub with no shared wizard" — plan's regression target "rests on a wrong premise". P12: "approach VR baselines NOT regenerated — VR is `continue-on-error`" (deferred to #82). P13: worktree lacked node_modules "(memory `local-agent-visual-gate-notes`)". | no |
| `v3-component-library-grid-report.md` | #79 | P6, P7, P12, N | P6: Q3 conditional "never fired", spec untouched "as planned". P7: `.capability` card vs AC 3 disabled state; press-all vs Non-Goals and read-only spec. P12: `loc-summary.json` in commit "which the plan did not anticipate" — drift flagged on first staged run. N: Docker file sharing "broken for `~/Desktop`"; stranded fix `85b3689` on another branch. | no |
| `v3-craft-sweep-audit.md` | #82 | P4, N | P4: F-2 "first measurement of this was wrong" — wrapper made probe PASS "and the defect was still there". N: O-1 fieldwork zero focusables flagged for owner, not fixed. | no |

## Code frequency (37 reports)

| code | reports |
|---|---|
| P1 plan-literal-wrong | 22 |
| P2 paid-or-owner-step-not-run | 1 |
| P3 stale-citation-propagated | 3 |
| P4 gate-red-proof-mismatch | 10 |
| P5 sibling-tree-contamination | 10 |
| P6 deviation-noise | 16 |
| P7 plan-internal-contradiction | 15 |
| P8 plan-missed-existing-state | 22 |
| P9 explicit-rule-ignored | 3 |
| P10 unrequested-addition | 12 |
| P11 validation-claimed-not-run | 4 |
| P12 generated-artefact-drift-missed | 9 |
| P13 known-trap-not-in-plan | 13 |
| N other | 24 |
| no codes | 1 (`studio-249`) |
| PARTIAL = yes | 1 (`studio-epic-close-223`) |

## Recurring "N other" (≥2 reports)

1. main/origin advanced after the plan was written; base SHA, line anchors, group numbers or block names re-derived (#42, #264, #207, #253, #251, #218, #221, #208, #203, #80).
2. Pre-existing defect found while validating, flagged for its own ticket rather than fixed (#217, #207, #216, #210, #82).
3. Cleanup `pkill` too broad — killed or may have killed a sibling session's server (#25, #223).
4. saulera pack's unshipped `fonts/fonts.css` `@import` 404s; exempted or avoided (#213, #222; also #207, spike 3).
5. A plan step deferred to post-merge (owner comment / doc amendment) still pending at report time (#6, #244, #216).

Also seen twice: a PR review's suggested fix was wrong and rejected with measurement (#217, #244); `npm run test:docker` named in two plans does not exist (#216, #219 — counted under P1).
