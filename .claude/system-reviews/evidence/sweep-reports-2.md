# Sweep — reports slice 39–76 (38 files)

Source: `.claude/reports/*.md` sorted, lines 39–76. Read in full. Codes per taxonomy P1–P13, N.

| file | ticket/PR | codes | evidence | PARTIAL? |
|---|---|---|---|---|
| discovery-strict-mcp-352-report.md | #352 + #353 | P1, P8 | P1: label "30.26, not 30.23 … as #353 guessed". P8: "#287 landed 30.23–30.25 after #353 was filed"; fix bullet's preflight "never spawns the CLI, so its root cannot answer". | no |
| discovery-warmup-refusals-343-report.md | #343 / PR #344 | P5, P6, P8, P10, N | P5: "discovery/allergen-matrix-1/ (another session's untracked run)… Staged by explicit path". P6: "D2 Step 2 dropped, per the ticket's own rule". P8: "the ticket's step 1 mechanism does not exist in this SDK"; "'the CLI denies those itself' … is not what the files show". P10: D4 bracket also gated PostToolUseFailure — "Reversed in review round 1 (F1)"; D5 probe gained `denied` field. N: paid probe ran but "the warmup made none" — run-time premise "not yet observed". | no |
| discovery-whole-bank-opus-posture-report.md | no ticket / epic #279 | P8, P10, N | P8: ticket said a new depth "fails by name"; "observed on the unedited gate it would not". P10: D1–D3 "additions, each one gate line — drop any on triage". N: "Base moved during review… every gate below was re-run on that merged tree"; paid comparison run left as owner's decision. | no |
| docs-chain-example-field-demo-notice-211-report.md | #211 | P1, P4, P7, P8, P10, P11, P13 | P1: "plan predicted staying inside 25,700" — landed 25,800. P4: 18B branch "silently vacuous… Replaced with a synthetic stripped pack". P7: "Task 4's VALIDATE command and Task 10A … contradict Task 3's own DO-NOT-IMPLEMENT". P8: "group count lives in four places, not the one Task 11 names"; header index "16 and 17 missing entirely"; renderer header "seven templates" stale. P10: added index lines 16/17 beyond "do not re-litigate". P11: "VR evidence was an update:docker run… it cannot fail, so '20/20' … was not a gate result"; parser refusals "exercised by nothing" yet COMPLETE claimed. P13: porcelain staged-drift false failure; first update:docker "rewrote no baselines at all" — both known memories, not in plan. | no |
| ds-list-row-primitive-report.md | #101 | P5, P6, P7, P10, P13 | P5: shared dir "carrying 5 staged files (157 insertions) of another ticket's in-flight work". P6: "Used the plan's 'simpler equivalent'"; "No northwind KPI tool committed. As the plan directed". P7: plan clause "(or, on a row, its meta)" leaks the answer "against the task's own GOTCHA". P10: narrow-width flex-wrap fix "beyond the plan's block"; CSS banner line. P13: "the known sub-perceptual skip" forced rm; drift-check staged false positive "Same mechanism as the recorded mid-merge false positive". | no |
| dtcg-inversion-token-source-report.md | #2 | P6, P8, N | P6: "site.js line 2 kept as-is… per byte-surgical instruction". P8: plan's cited guard pattern "silently broken on this machine: the repo path contains a space". N: skill default feature-branch vs "project CLAUDE.md pins 'work on main'". | no |
| epic-1-ticket-audit.md | epic #1 audit | P5, N | P5: "Uncommitted work in the shared working dir, existing NOWHERE in git" (components.css, specs, pack.json, arch doc). N: PRs #19–#21 "merged into feature branches after those bases had already gone to main"; "review rounds exist only as untracked .claude/code-reviews/*.md files"; spike-5 verdict "never posted to issue #5"; README regex stale vs recorder. | n/a (audit) |
| factory-copy-inspect-panzoom-173-report.md | #173 | P6, P8, N | P6: "plan's scratch drivers stayed scratch (as specified)". P8: memory-sourced "expectation of ERR_CONNECTION_REFUSED noise… did not reproduce". N: "original session idled before CLAUDE.md, the functional drivers, the report"; stacked merge on #176. | no |
| factory-intake-wizard-reskin-report.md | #10 (10.2) | P1, P7, P8, P10, P13 | P1: "`capability live` count is 5, not the plan's expected 4… undercounted". P7: root-absolute imports "makes the plan's own Task-2 Node-parse VALIDATE gate impossible". P8: re-render per change would close the native colour picker (input fires continuously). P10: "Review" scrolls instead of dead-ending; preview padding scoped. P13: plan's Level 3 "host-npm test path… would fail by design (macOS vs Linux)". | no |
| factory-integration-toggle-ethics-trace-report.md | #10 (10.3) | P1, P2, P5, P6, P8, N | P1: mask selector `iframe.factory-embed` has "no box to mask" when hidden; "plan's stated count of 1 counted only the div". P2: "Phase 5 / Task 12 not executed" — new trace "~$0.45×N… user-gated", offered as follow-on. P5: "Pre-existing / unrelated, do not stage: docs/epics/…architecture.md… per-company-brief.architecture.md". P6: `<button aria-pressed>` "a plan-sanctioned option ('either is fine')". P8: trace player's document-level ←/→ hijack "not in the written plan", advisor caught. N: comments "tripped the Level-1 anti-grading grep… Reworded". | no |
| figma-drop-portal-ui-report.md | #116 | P1, P6, P8, P10, P13 | P1: `pattern="[a-z0-9-]{1,40}"` "specified verbatim in the plan… is invalid"; header baked path "in TWO places, not one". P6: "400 → 500… recorded in the plan itself". P8: plan base branch "had merged to origin/main (PR #118) and moved 3 commits past it"; `.last-response.json` "the plan's setup notes don't mention". P10: two pre-existing `[hidden]` CSS bugs fixed; post-merge scale copy extended. P13: "fresh worktree has no tooling/style-dictionary/node_modules" (known). | no |
| figma-import-scales-and-dock-report.md | #121 / PR #120 | P7, P8, P13, N | P7: header clause vs AC7 byte-identity "Those two plan clauses contradict each other"; fixture "no shadows" vs Task 7 validate pinning `shadow-lg`. P8: "A third allowlist… The plan named two"; `loadContract` "was module-private"; `tooling/figma/maps/ did not exist". P13: `--color-accent` on index "is the hero's canned re-skin, not the pack's" (known trap). N: system-graph left at three packs to avoid VR churn — follow-up. | no |
| figma-token-import-report.md | #110 | P2, P8, N | P2: "The parity artifact stays unwritten (owner's call)"; needs a human step in Figma. P8: handover's `/styles` route "returns an empty array… figma-parity.mjs's own comment already said so". N: handover's FIGMA_TOKEN-leak claim "the owner confirmed that is wrong"; `tokens.verdant.css` "is stale… no gate catches it". | no |
| floor-into-instance-prototype-slot-reader-adjust-report.md | #89 / PR #100 | P1, P5, P6, P8, P10, P11, P12, P13, N | P1: "Ported agentic-ui-study.html:35–76, not 28–76"; "the 8 (actually 9) VR pages". P5: main dir "holds ~44 staged-but-uncommitted doc files — three… STALE copies". P6: "Took the plan's OPTIONAL capability badge"; headers "if warranted". P8: plan left Station 5 prose "that the change makes false". P10: badge withdrawn by JS (dev 9), `_headers` rules, `views` return. P11: four-state table "had recorded the claim as '—', i.e. withdrawn, when it was retained verbatim". P12: "plan's 'AC #4 — N/A, no baseline regen' was too broad… missed loc-summary.json… It did drift". P13: "Fresh worktree needed… npm install (known, per repo memory)". N: review's "suggested one-line guard… would NOT have closed the hole". | no |
| floor-runner-parameterize-composition-spike1-report.md | #88 | P6, P10, N | P6: shape refinements "within open-Q #4's mandate"; Fieldwork "NOT migrated… per the plan's assumption #3". P10: `today` coherence check "the plan listed today as a key without a coherence check". N: epic docs "currently untracked… Whether to land both… is a maintainer call". | no |
| handoff-data-layer-report.md | #7 | P1, P5, P8, N | P1: jobs folder path in L3 command "needed adjusting". P5: "working tree carried uncommitted CLAUDE.md lines from other in-flight tickets". P8: kb-format.md "was untracked, not just outdated". N: "A concurrent session… switched HEAD… mid-implementation, so the atomic commit initially landed there". | no |
| handoff-pack-viewer-report.md | #14 | P6, N | P6: bundle format "the plan's recommended resolution… Recorded, not a deviation"; no commit "the plan's GOTCHA sequence, not a gap". N: "Base branch carries 3 unmerged #9 commits… Do NOT rebase onto main now". | no |
| home-uplift-169-report.md | #169 | P5, P6, P8, P12, N | P5: main checkout "held #170's uncommitted work". P6: `<length-percentage>` "the plan's own pre-authorized widening"; glosses "the plan allowed". P8: `tokens.plusui.css` "the dock's 4th pack, missing from the plan's command". P12: "A fifth baseline event" — 24 CSS lines flipped loc-summary; approach recaptured after the proof. N: "Port 4757 was held by the parallel #170 session's server — the first functional run silently tested the WRONG tree". | no |
| inspect-engine-166-report.md | #166 | P1, P6, P8, P10, P13, N | P1: plan's `dataset.inspect = "on"` made root match trigger — "unknown id 'on' on every activation". P6: worktree "the exact case the plan's risk section says to take". P8: "no `.btn-ghost` exists in components.css… no `.mt-md` utility". P10: overflow containment, top clamp, anchor gating. P13: "Fresh worktree needed npm ci… (known)". N: epic docs "untracked in this worktree and must be committed in this PR". | no |
| live-derivation-engine-report.md | #3 | P1, P5, P7, P8 | P1: "plan's prose said '40+'; 46 is the actual contract count"; `fg-muted` l 0.55 "yields ~4.38:1". P5: "Pre-existing uncommitted README.md edit (1 line, not mine)… unrelated untracked mcp/ scripts". P7: "plan values could not clear their own pairs"; accent/bg-inverse pair "mathematically unsatisfiable". P8: `derive.html` needs portfolio.css — "plan said skip" but classes styled there. | no |
| minimap-caption-a11y-273-report.md | #273 | P11, N | P11: "The update run's own pass is the suite evidence" (update runs cannot fail). N: serve on 4763 "killed externally mid-run (parallel-session hazard)"; main moved post-#274–#276, rebased; chromium frame flake under load. | no |
| on-dark-accent-token-report.md | #17 | P1 | P1: plan said `#ffd400` would "exit immediately"; "in fact it lightens 0.54 → 0.62". | no |
| pack-seed-derivation-vision-run-report.md | #40 | P1, P5, P7, N | P1: band "[1.05, 1.7], not the plan's [1.1, 1.6]" — engine ramp outside plan band. P5: "the shared working dir's branch changed under me (a live parallel session)". P7: "The plan is internally inconsistent: … 'auto-fill ANY missing token' … yet 'a seed missing color-accent throws'"; Task 10 vs AC3 proposed-vs-truth. N: arch doc "also exists, with different content, on fix/privacy-guard…" — add/add pending. | no |
| param-count-manifest-generator-report.md | #167 | P1, P8, N | P1: plan's single /build selector "would have been a duplicate". P8: "the plan's `.brand-import-report` class doesn't exist". N: "measured 62 already exceeds epic #164's '≥40 from ~20' target… needs an owner decision". | no |
| peak-region-audit-262-report.md | #262 | P8, P11, P12, N | P8: issue premise "instance.html still renders .peak-note" — "confirmed stale". P11: token-lint "is a verify step and it is not in this report's gate list — it was never run locally". P12: approach baselines "genuinely stale" and orphan-token cascade both "forced by CI". N: "Flag 3's stated cost did not exist" — factory PNGs byte-identical; deferral reasoned, not measured. | no (amended) |
| per-company-build-unlisted-deploy-report.md | #44 | P2, P5, P7, P8, P10 | P2: "one live-header curl this sandbox physically can't do… AC #2 inferred"; decision "provisional". P5: "never -A, given the untracked .claude/** and __Final_phase.md". P7: Phase 2 step 6 implements `--public-origin` vs "the plan's own Out of Scope… govern". P8: CLAUDE.md map line "still claimed #44 'rewrites… via one inline line'… false". P10: 5th anchor `#instance-name`; validator `hidden` guard; comment cleanups. | yes |
| portability-proofs-report.md | #12 | P2, P8, P10, N | P2: "no FIGMA_TOKEN… real run pending… commit deliberately omits Closes #12". P8: "scenarios/ does not exist in this worktree" — commit not an ancestor of base. P10: parity field "computed… rather than hardcoded null". N: "stacked on feature/handoff-data-layer… PR will transiently contain #7's commit". | no (AC open) |
| portal-origin-guard-report.md | #157 | P4, N | P4: fourth mutation "first came back green, because the perl pattern silently failed to match". N: "the claim reached this PR's body before it was checked" — `receiveExport` finding withdrawn after driving the path. | no |
| portfolio-motion-phase02-report.md | v3 motion phase 02 | P1, P2, P5, P6, N | P1: plan's card-1 line "'5 stations · 1 live today' is FALSE". P2: "Outstanding — human Level-2 pass… Chrome + Firefox + Safari". P5: "other dirty .claude/ files… pre-date this work — do NOT fold them". P6: spring split "its own gotcha offers a calm fallback. I took the fallback". N: plan comment text "0ms… itself trips the no-literals grep"; branched off "unmerged and unpushed" phase01. | no |
| portfolio-motion-phase03-factory-showpiece-report.md | v3 motion phase 03 / PR #55 | P1, P2, P5, P8, N | P1: "not the input's stale `--motion-duration-*`" token names. P2: "AC #5 — Safari + Firefox. Only Chromium was driven"; AC #8 drag feel to user. P5: parallel session "switched the shared tree's branch… leaving my Phase-3 edits stranded". P8: plan assumption #2 (gate captures under reduced-motion) "is factually wrong". N: "first (shared-tree) baseline regen ran against its in-flight portfolio.css, producing a polluted diff". | no |
| portfolio-motion-phase04-visual-richness-report.md | v3 motion phase 04 | P1, P2, P6, N | P1: plan cited "::backdrop, portfolio.css:330 by line number; the inserted blocks shift that line". P2: "Safari eyeball outstanding: the plan's judgment gate names Chrome and Safari". P6: PR #55 already merged — "contingency was satisfied by construction". N: first update:docker "churned only 8 baselines, not the predicted 12" — sub-perceptual; forced by rm. | no |
| portfolio-motion-phase05-utility-dock-report.md | v3 phase 05 (PRs A/B/C) | P1, P2, P4, P10, P12, P13, N | P1: plan selector `link[href*="/system/tokens."]` "would match tokens.contract.css". P2: "Safari eyeball not done… the plan calls for a real-Safari pass". P4: "C2 drill mutation. `/* x */` can't trip the scanner". P10: deep-link focus re-assert; keyframe panel entrance. P12: "loc-summary cascade (A and C, not in plan)" → approach baselines. P13: "`--update-snapshots` alone rewrote nothing (known repo memory)". N: "PR C branched from A's branch, not main". | no |
| pr-156-review-fixes.md | PR #156 / #140 | P4, P10, N | P4: drawer script "caught a flaw in itself first: step 5… re-checked an already-selected radio, which fires no change". P10: "Fixed, wider than specified" — `runOptions` split. N: review's fix "lands only at server.mjs:105… the only check available… is a grep"; Origin guard deferred to #157. | no |
| pr16-review-lows-report.md | #18 (PR #16 lows) | N | N: "crude `body.textContent.includes("FAIL")` probe… false positive (it matched a script-string constant)". | no |
| private-instance-shell-report.md | #43 | P5, P7, P10, P13 | P5: "Pre-existing untracked files predate this work and must stay out… `__Final_phase.md`". P7: Task 6 "matching factory Station 5's register" vs "the plan's OWN honesty audit (NOTES) says… replay register"; esc() copy vs mirrored module has none. P10: `.cs-jump` nav + framing paragraph "Not specified line-by-line"; ported Phase-3 motion CSS. P13: approach PNGs "rm-ed… (per the vr-update-skips-subperceptual memory)". | no |
| protos-bus-toggles-device-frame-176-report.md | #176 | P1, P4, P5, P6, P7, P8, P10, P13, N | P1: "plan predicted 19500–19700… one 100-line bucket above". P4: driver "briefly a check that could not fail" — lazy iframes vacuous; readyState wait wrong; noise filter too broad. P5: shared checkout "grew nine modified files belonging to other tickets". P6: native radio "plan OPEN QUESTION 6". P7: plan ordered Task 12 before 13–15 "while Task 12's and Task 15's own GOTCHAs both say baselines must come last"; unknown-slot AC "unsatisfiable". P8: `TONES` "never exports it, so that import would have failed". P10: `buttons` guard (Firefox drag bug). P13: fresh worktree npm ci "Known". N: stale 4757 server "silently invalidated my first CSS verification pass"; "implementing session went idle… a finishing session completed". | no |
| protos-pack-skin-inspect-175.md | #175 | P1, P6, P8, P10, P13, N | P1: "the plan's 'total 65 → 68' is stale: the real numbers are 72 → 74"; factory churn predicted, "did NOT churn". P6: approach not regenerated — "The plan sanctions this branch explicitly". P8: home toggle "was not missed — … in the manifest since #166"; kill-switch "not loaded on the proto pages"; inspect "wires its triggers once" — "a real defect the plan would have shipped". P10: inspect gated to top window; `.proto-head-tools`. P13: "`gen-loc-summary --check` gave a false pass before staging… exactly memory". N: "Four of five initial cross-engine failures were harness bugs". | no |
| public-drop-to-reskin-report.md | #130 | P1, P4, P5, P7, P10, P13, N | P1: plan's `VALUE_OK` regex "rejects every font stack… and the three `linear()` easings"; "plan estimated ~12,900" vs 13,700. P4: plan's `git diff --stat system/tokens.plusui.css` check "is vacuous because nothing on this branch regenerates that pack". P5: "A parallel session is editing this shared working directory… index.html was rebuilt as HEAD + my two hunks". P7: "The plan contradicted itself here (Task 10 decision 5 vs Task 16 step 10)". P10: `clearInlineTokens()` "not in the plan"; private-mode apply. P13: hero canned re-skin masked the import (known inline-vs-stylesheet trap); approach PNGs forced (memory). N: "first VR run had baked their roundtrip.html copy into" baselines; "Port 4747 was already serving the wt-figma worktree's portal"; `tokens.verdant.css` "pre-existing staleness on origin/main… Worth a ticket". | no |

## Code frequency (38 reports)

| code | count |
|---|---|
| P1 plan-literal-wrong | 19 |
| P2 paid-or-owner-step-not-run | 8 |
| P3 stale-citation-propagated | 0 |
| P4 gate-red-proof-mismatch | 6 |
| P5 sibling-tree-contamination | 15 |
| P6 deviation-noise | 15 |
| P7 plan-internal-contradiction | 10 |
| P8 plan-missed-existing-state | 23 |
| P9 explicit-rule-ignored | 0 |
| P10 unrequested-addition | 17 |
| P11 validation-claimed-not-run | 4 |
| P12 generated-artefact-drift-missed | 4 |
| P13 known-trap-not-in-plan | 12 |
| N other | 29 |

PARTIAL: 1 (per-company-build-unlisted-deploy). Portability-proofs is COMPLETE by status but leaves #12 open on the real-run half.

## Recurring "N other" (≥2 reports)

1. Parallel session's server on a shared port served the wrong tree, or was killed mid-run (#169, #176, #130, #273).
2. Branch stacked on an unmerged sibling branch; PR base deferred to PR time (#14, #12, phase02, phase05, #173/#176).
3. Concurrent session switched the shared tree's HEAD mid-implementation; commits/regens landed on the wrong branch (#7, phase03, #176).
4. Gate grep tripped by comment or prose text; comments reworded to pass (10.3, phase02).
5. Reviewer's suggested fix inadequate; implementer widened it (PR #100/#89, PR #156).

Also ≥2: implementing session idled, a finishing session completed the ticket (#173, #176); pre-existing stale `tokens.verdant.css` with no gate (#110, #130); untracked governing epic docs must ride the PR (#88, #166, #40); harness/probe false positives separated from defects by re-running against origin/main (#18, #175, #169).
