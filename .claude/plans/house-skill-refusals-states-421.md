# #421 — house skill: six refusals, four more states, two documented disagreements

Text only, inside `.claude/skills/portfolio-design/`. From the 2026-09-15 borrowability audit.

1. `references/CHECKLIST.md` gains a "Refusals" section: six NEVERs, each with its source repo and
   licence in a trailing parenthesis, plus the two *Rejected, and why* notes where the rejected
   rules would sit (numbered stations; the hero stat as the Hooked self-reward).
2. `references/CRAFT.md`'s "Interactive states" gains permission-blocked · timeout · stale data ·
   interrupt-and-resume, one sentence each on what the surface says.
3. `SKILL.md`'s Lineage names the three sources once.
4. Grep before adding: the measure, the tracking floor, `::selection`, scrollbar theming, tabular
   numerals, the em-dash ban — present ones not duplicated, absent ones not added (out of scope).
5. The manual eyebrow check on `index.html` and `approach.html`: look, decide, record in the PR body.
→ verify: `node tooling/build-checks.mjs` unchanged (36/36); `git diff --stat` touches only the skill.
