# Implementation Report — Pack-seed derivation (spike 1: Verdant round-trip)

**Plan**: `.claude/plans/pack-seed-derivation-vision-run.md`   **Branch**: `feature/pack-seed-derivation-vision-run` (worktree `ux-factory-wt-40`, off `main`)   **Status**: COMPLETE (AC4 provisional). Scaffolding + de-risk + the **real vision run** (clean on the 3rd pass, ~$1.62 total) + curated/validated trace + proposed CSS + fidelity diff + the **human gate** (Linards approved; corrected `color-accent`) are all done and committed. The **offline real-product half of spike 1** (§Spikes / AC4) is deferred as a documented follow-up per the plan's fallback — the label lands **provisional** on the Verdant round-trip with the controlled-case caveat stated (it cannot be committed here: privacy boundary forbids real-brand data in this repo). All three CI gates pass.

## Merge note (for the reviewer / merger)
- **This PR introduces `docs/epics/per-company-brief.architecture.md` to `main`** (+74 lines; `main` does not yet track it — #40 owns it per the plan). That is why a "pack-seed derivation" PR carries an architecture doc: §Spikes 1's outcome must land in it (AC4).
- **Cross-branch duplication:** the same file also exists, with *different* content, on `fix/privacy-guard-physical-containment`. Whichever branch merges second hits an add/add conflict on this file — reconcile the two edits at that merge (not a #40 defect).
- **AC4 is provisional:** the offline real-product half of spike 1 is a deferred follow-up (privacy boundary forbids committing real-brand data); tracked under the arch doc's §Open questions.

### Real-run outcome (Tasks 8–10)
- 3 passes to a clean PIV trace (the expected tighten-and-re-run loop). Pass 1 wrote the seed under the wrong phase (no `[[piv:implement]]`); pass 2 fixed phases via an explicit 4-phase walkthrough but a forceful "don't orient" preamble *primed* pre-plan exploration (2→6 null-phase steps); pass 3 switched to positive framing (mirroring `record-trace.mjs`) → clean: `plan→gate→implement→validate`, 0 null-phase, 1 artifact, 2 denials (fence active). Never hand-edited the JSONL.
- Proposal: 37 perceptual tokens (0 missing, 0 unknown). Accent `#2d6a48` vs ground truth `#2f7a4d` → OKLab ΔE **0.05** (at the threshold — an honest slightly-dark read, not a rigged near-zero); type/spacing/radius usable; proposed palette AA **12/12**. **Verdict: `agent-proposed, human-approved`** (with the closed-round-trip caveat).
- `traces/pack-seed-verdant.{raw,}.jsonl` curated + validated (matching sessionId). `tooling/round-trip/{verdant.seed.json, tokens.verdant-proposed.css, verdant.diff.json, README.md}` generated.
- **Human gate (done).** Linards reviewed the proposal, set `review.approved = true`, and corrected exactly one token — `color-accent` — which the agent read slightly dark (`#2d6a48`). The correction (`→ #2f7a4d`) is recorded in the seed's `review.corrections`; the trace preserves the raw proposal; `changedTokens` matches the actual diff (verified 1/37 tokens changed). This is the visible "agent proposes, human decides" boundary.

## Summary
Built and proved the entire deterministic half of the capability that turns product screenshots into a proposed design-token pack: a reusable pack emitter, the canonical Verdant ground-truth pack, the perceptual-fidelity diff (the spike-1 measurement), the one-time round-trip screenshot capture, an image-hygiene hardening of the trace recorder, and the recorded vision-run harness itself. Phase-0 de-risk confirmed live: the Agent SDK's `Read` returns usable image blocks (vision viable) and auth works via the Mac's Claude CLI login. The `--dry` smoke exercises the real fence + a real screenshot Read + a denied ground-truth read + a seed Write + a gen-pack-css compile, cleanly (0 null-phase). What remains is the **real** token-spending run, its **human gate** (Linards approves/corrects values — cannot be self-satisfied without faking the "agent proposes, human decides" exhibit), and the **real-product** fidelity number.

## Tasks completed
- Task 0 — de-risk vision + auth → `tooling/round-trip/probe-vision.mjs` (CREATE); ran ✓ (agent described the PNG, saw an image block).
- Task 1 — export `cssValue` → `agent-layer/gen-token-css.mjs` (UPDATE); `--check` shows no drift.
- Task 2 — DTCG/flat → pack emitter → `agent-layer/gen-pack-css.mjs` (CREATE); unit-tested (flat+DTCG identical CSS, auto-fill, throws on unknown/malformed).
- Task 3 — canonical ground-truth pack → `system/tokens.verdant.css` (CREATE, generated via `--verdant`); byte-stable regeneration; token-lint ✓.
- Task 4 — fidelity diff → `tooling/diff-pack-seed.mjs` (CREATE); proven on truth (PASS, accent ΔE 0) and wrong (label flips) fixtures.
- Task 5 — round-trip capture → `tooling/visual-regression/capture-roundtrip.mjs` (CREATE) → `tooling/round-trip/input/verdant-plant-overview.png` + `verdant-full.png` (CREATE); green re-skin visually confirmed.
- Task 6 — image-response hardening → `portal/lib/trace-recorder.mjs` (UPDATE `capResponse`); confirmed in `--dry` (Read step = `[image tool_response: 1 image block(s) — omitted]`, not a base64 blob).
- Task 7 — vision-run harness → `portal/record-derivation.mjs` (CREATE); `--dry` clean (plan→gate→implement→validate, 0 null-phase, fence denies the ground truth, seed compiles).
- Governing docs carried onto the branch: `docs/epics/per-company-brief.architecture.md` (was untracked; #40 owns it — not on any ref), `.claude/plans/pack-seed-derivation-vision-run.md`.

## Tests added
No unit-test suite (project rule: "run the surface you touched"). Deterministic checks, all green:
- `gen-pack-css`: flat `derive().tokens` and the equivalent DTCG seed emit **identical** 47-token `:root`; a partial seed auto-fills the remainder; unknown token name / missing `$value` / empty value each throw and name the token; no `[object Object]`.
- `diff-pack-seed`: fixture == ground truth → `agent-proposed, human-approved` (accent ΔE 0, type/spacing usable, AA 12/12); magenta-accent + 30px-body fixture → `human-authored with agent assistance` (accent ΔE 0.46, type unusable). Decision rule proven before any agent output.
- `record-derivation --dry` (real fence, real screenshot, real compile): 0 null-phase, 1 denial (ground truth), 1 artifact, image placeholder present, synthetic secrets redacted. ~$0.10.
- `probe-vision`: Read tool used, image block seen, real description returned → Read-based vision viable (no pivot to prompt-embedded images).

## Validation results
- `node agent-layer/gen-token-css.mjs --check` → ✓ no drift (export left the default path byte-identical).
- `node agent-layer/gen-pack-css.mjs --verdant` → 47 tokens; regenerate + `git diff` empty (byte-stable).
- `node tooling/token-lint.mjs` → ✓ (47 contract · 0 undeclared · 0 orphan; packs excluded).
- `node --check` on all new/edited `.mjs` → ✓.
- CI gates (Task 12), run end-to-end on the committed trace + artifacts: `node tooling/drift-check.mjs` → ✓ `syntax · token-css · handoff · scenarios · traces`; `node tooling/token-lint.mjs` → ✓ (47 contract · 0 undeclared · 0 orphan); `node tooling/validate-trace.mjs` → ✓ `pack-seed-verdant` raw + curated (14 steps · 4 phases · 1 artifact · matching sessionId), and `demo-notice` still ✓ (the `capResponse` hardening is text-path-identical). Visual gate not run locally (Linux-baseline; macOS platform-diffs) — safe: no shipped page or baseline touched.

## Deviations from the plan
1. **`gen-pack-css` completeness semantics.** The plan is internally inconsistent: it says "auto-fill ANY missing token, don't hardcode a statics list" (3×) yet also "a seed missing color-accent throws." I followed the dominant instruction — auto-fill any of the 47 missing from contract defaults — and made the hard throw-guards **unknown token name** and **malformed value** instead. Honesty is preserved because the CLI **reports every auto-filled token**: an omitted *perceptual* token surfaces in that report (the run's self-correction signal), rather than being silently injected as a neutral default. This keeps the drift-checked `gen-token-css` default path untouched (AC2's "gen-token-css path" honored via the exported `cssValue`).
2. **Type-ratio usability band `[1.05, 1.7]`, not the plan's `[1.1, 1.6]`.** The engine's own comfortable ramp has adjacent ratios of **1.083** (13→12 tail) and **1.581** (49→31 head) — both outside `[1.1,1.6]`. A strict band would false-fail by-construction-usable ground truth (breaking the fixture==truth PASS test). Band calibrated so the engine's ramp passes with margin while still catching degenerate ramps (duplicate/inverted steps, wild jumps). Recorded in `diff-pack-seed.mjs` with the reason.
3. **Branch = isolated worktree off `main`, not `checkout -b` in the shared dir.** The plan said "off main, not `feature/factory-integration`." During orientation the shared working dir's branch changed under me (a live parallel session) — so a worktree (`ux-factory-wt-40`) is the safe realization of that instruction; it never touches the shared dir's HEAD.
4. **The fidelity diff measures the agent's *raw* proposal, not the human-approved seed — a correction to plan Task 10.** Task 10 says "generate `verdant.diff.json` from the approved seed," which is honest only while the gate leaves *measured* tokens untouched. Here the gate corrected `color-accent` to exact ground truth, so diffing the approved seed made `accent.proposed` report `#2f7a4d` at **ΔE 0** — a field literally named `proposed` holding a human value and a fake-perfect fidelity (violates AC3 "*proposed* vs ground truth" and the hard honesty contract; the spike measures *proposal quality*, never who decides). **Fix:** `verdant.seed.json`'s token values are restored to the agent's verbatim proposal (`color-accent #2d6a48`, lifted from the trace's Write step); the human's correction is recorded in `review.corrections` instead of overwriting the measured value; the diff + proposed CSS regenerate from it → honest `accent.proposed #2d6a48, ΔE 0.05`. This matches the plan's own description of `verdant.seed.json` ("the agent's proposed seed … carrying an explicit review block") and `diff-pack-seed.mjs`'s header ("measure how close the vision agent's **PROPOSED** pack seed gets to ground truth"). Verdict unchanged (0.05 ≤ 0.05 threshold): "agent-proposed, human-approved."

## Issues encountered
- **First `--dry` had 1 null-phase step** — the agent ran `ls` to orient before `[[piv:plan]]`. Root cause was my task wording ("Glob first if needed"). Fixed by adding the "no orientation first" guard + naming the exact screenshot file (mirroring `record-trace.mjs`'s `--dry`). Re-run is clean. The real task got the same treatment (names both screenshot files) to keep the real trace null-phase-free.
- **No `portal/.env`** — auth falls back to the Mac's Claude CLI login, which the spawned SDK subprocess inherits (probe + `--dry` both succeeded). No token needed for this environment.

## Remaining work
- **Tasks 8–11 (done).** Real vision run + human gate + curate/validate trace + proposed CSS + fidelity diff + §Spikes provisional label — all landed and committed on this branch.
- **Task 11 — offline real-product half (deferred follow-up, not dropped).** One real product's screenshots + published tokens are needed, and by the hard privacy boundary **nothing real-brand may be committed to this public repo** — so this is run locally by Linards and only its numbers land in §Spikes. The label is therefore **provisional** on the Verdant round-trip with the controlled-case caveat stated (plan's explicit fallback). Tracked as the first item under the arch doc's §Open questions; fold the numbers in to finalize the label.
- **Task 12 (gates + PR).** drift-check ✓ · token-lint ✓ · validate-trace ✓ locally. The visual gate is **not run locally** (its baselines are Linux; on macOS all 16 shots platform-diff — false failures) but is **safe**: this branch touches no shipped page and adds no baseline (`system/tokens.verdant.css` is a new pack not loaded by the shell; the round-trip capture is out-of-band, verdant not added to `PACKS`). CI runs it on Linux → still 16 shots.
