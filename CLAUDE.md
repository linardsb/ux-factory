# CLAUDE.md — ux-factory

## What this is
The code repo for an AI-first UX-engineering portfolio platform: a token-contract design system that re-skins an entire site from one line of CSS, plus the build-time "factory" around it — Node ESM generators and a local portal workbench — being extended into a pipeline that runs visibly in front of the reader (intake → generated design system → data-connected prototype → handoff pack) so hiring managers can verify senior UXE skill instead of trusting claims. This repo holds code only; per-application content, the pipeline skill, and the knowledge base live in the sibling jobs folder (`../Linards jobs folder/_factory/`). Intent and decisions: `docs/epics/`.

## Architecture map
```
system/                     the shipped design system — brand-agnostic core + packs
  tokens.source.json        DTCG source of truth — gen-token-css.mjs generates the two CSS layers below from it
  tokens.contract.css       layer 1 (GENERATED): every semantic token components may use, with neutral fallbacks; no brand ever
  tokens.neutral.css        layer 2 (GENERATED): the neutral pack; a company build clones it to tokens.<company>.css
  components.css            layer 3: token-only components — no literals, render identically under any pack. Site CHROME that must reach the proto pages lives here too, not in portfolio.css, because the protos load this sheet and not that one: the ⌘K palette (#168), the appearance dock + ruler and the inspect bubble (both moved in by #175). portfolio.css's global reduced-motion kill-switch does not reach here, so a chrome block carries its own
  portfolio.css / proto.css surface styles for portfolio pages / prototypes
  site.js                   injects chrome (header/footer/nav) from window.CLIENT_CONFIG
  pack-boot.js              pre-paint pack restore on all eleven shipped pages (classic script; the two protos joined at #175, components.html at #215): re-points the one tokens.<pack>.css head line from localStorage, hard-allowlisted, guaranteed no-op by default (VR relies on that), and its tag must stay the LAST element in every head that carries it (portfolio-ux-uplift §Phase 5)
  dock.mjs                  appearance dock (hand-written canon): pack switcher across the packs in its own hard-allowlisted `PACKS` list — neutral · saulera · verdant · plusui as of #129, and adding a pack means editing that list AND `PACK_RE` — plus copy-tokens + left scroll ruler; hash-routed #appearance disclosure, persisted choice restored by pack-boot.js. On all eleven shipped pages (the last, components.html, at #215), but the two protos import it only when `window.self === window.top`, so work.html's iframe embeds get the reader's pack without a nested rail (portfolio-ux-uplift §Phase 5)
  analytics.mjs             CF Web Analytics beacon (public token + production host, both filled at launch; node-safe behind a `typeof document` guard, because tooling/build-checks.mjs imports two modules that import this one) + the virtual-route event helpers — CF WA has no custom events, so an event is a pageview at a SYNTHETIC PATH: /factory/{driven,built,shared,arrived} on home, /build/{pattern,shared} on the builder. Each fires ONCE from its own success path — never from a slot or a settled-state flag that also runs when the thing did not happen — and each path is a static literal, because the path is the entire payload. The `/build` pair share one restore, `flipTo`, which handles two collisions with the 50 ms window that the four `/factory` trackers do not. It puts the HASH back live — a disclosure opened inside the window (the dock) would otherwise be left open with no `#appearance` for Escape to match, measured on all three engines (#149). And it records the real URL ONCE across OVERLAPPING windows — a second flip opening inside a first one's 50 ms would otherwise snapshot the VIRTUAL path and restore the reader to it permanently, which 404s on reload; reproduced 4/4 on chromium, where the Copy click's own scroll is what starts the vocabulary fetch (PR #162). It also exports `onVirtualRoute()`, which is a CONTRACT and not a helper: `build-keep.mjs` builds the share link out of `location` and waits on it, because `shareUrl` only overwrites `?b=`, so a link built mid-flip keeps `/build/pattern` and 404s. The four `/factory` trackers still take a per-flip snapshot and read nothing live: both collisions are reachable there in principle, neither has been observed, and #149 deliberately did not touch them (tickets #6, #75, #77, #149; PR #162 review)
  derive.mjs (+ oklch.mjs, wcag.mjs, derive.rules.mjs)   view-time derivation engine: intake answers → token values + WCAG checks + ethics verdict; driven raw by /derive.html, spike runner in tooling/ (epic #1 ticket #3)
  agentic-renderer.mjs + action-bus.mjs   declarative agentic bridge (hand-written canon): vocabulary-validated {name,props,children} → real components (refuses everything else) + one bidirectional action contract (agent/click/keyboard, voice-ready); driven raw by /agentic.html (epic #1 ticket #11)
  trace-player.mjs          view-time trace replay (hand-written canon): parseTrace + renderTracePlayer render a committed trace as stepped cards in the four PIV acts; driven raw by /trace.html, designed surface = Factory page (epic #1 ticket #5)
  handoff-viewer.mjs        view-time handoff-pack viewer (hand-written canon): prepareHandoff + renderHandoffViewer show each component's spec head · engineer docs · agent vocabulary side by side from one generated source (markdown-subset prose renderer, exported to catalog.mjs since #215 — one renderer, two mounts); driven raw by /handoff.html, designed surface = Factory page (epic #1 ticket #14)
  catalog.mjs               the component catalog at /components (#215, hand-written canon): MOUNT 1 of "Docs, two mounts of one source" — renderComponentDocs draws ONE component's docs (playground driven through renderComposition on its own bus, code tabs SERIALIZED from the live render, API + token tables projected from prepareHandoff's three-artifact join, spec prose through the imported renderMarkdown, copy-as-Markdown = the committed spec file verbatim), and #218's studio inspector imports exactly that function. No generated catalog artifact, by recorded decision — the join is view-time. The one hand-written table is `WRAPPER_ATTRS` (vd-care-task-row observes `action`, the spec prop is `type` — a mechanical projection would fabricate an API), triple-pinned: build-checks group 21 checks it against the wrapper sources' observedAttributes AND the vocabulary, and tooling/catalog-journey.mjs pastes the serialized markup into the real element. The vd/react tabs gate on the pack's OWN portability list (3/17 pinned — the tripwire #220 tripped as designed; its ten ship wrapper-less); ⌘K's per-component commands are palette.mjs's STATIC `CATALOG_COMPONENTS` list, pinned against the vocabulary (the palette memoizes at first open, #188). Refusals are content in the per-component `role="status"` line, never throws; gates = group 21 + catalog-journey ×3 engines. MOUNT 2 LANDED AT #218 (`system/studio-docs.mjs`) and consumes FOUR exports and nothing else — `renderComponentDocs` · `headingTags` · `resolveTokenValues` · `watchPackSwap`. `opts` is spent: it carries `level` alone (2 → h2/h3 for this page, 4 → h4/h5 in the inspector, so the panel's own h3 title is not inverted), and the compaction is a CLASS the caller adds (`.cat-compact`), never a `compact` branch in the renderer — one code path for both mounts (epic #202 tickets #215, #218)
  annotated-source.mjs + glossary.mjs   legibility surfaces (hand-written canon): render build-time-extracted real-code snippets (system/annotated-source.json, generated by agent-layer/gen-annotated-source.mjs from its spec — drift-checked) + WCAG 1.4.13 term-definition bubbles on approach.html and, since #173, factory.html — initGlossary runs inside the graph mount module so an unknown key aborts before the ready handle and the VR gate fails loud (docs/epics/annotated-source-glossary.architecture.md)
  derive-probe.mjs          view-time probe beside approach's derive snippet (hand-written canon): runs the real derive() on a reader-chosen brand colour and reports the negotiated accent, the measured contrast pair + the engine's notes verbatim — the quoted code and the running code are one file
  board-ops.mjs             the studio's BUILD-OP vocabulary + a pure applier over breadboard.mjs's board model (hand-written canon, DOM-free): the eight ops (`place.add` · `place.rename` · `place.remove` · `affordance.add` · `affordance.rename` · `affordance.remove` · `connect` · `disconnect`), `applyOp`/`applyOps` (pure — a NEW board every time), `assertBoard`, and `parseOpCommand`. In `system/` because three layers need it: the fenced recorder's CLI, the generator's reproduce check, and #209's view-time driver. Two invariants: the caps are IMPORTED from breadboard.mjs, never re-literalled — and an op NEVER carries an id for the thing it creates (`place.add` takes a label), so ids are assigned here and gen-replay's reproduce check genuinely re-derives them instead of copying them out of the trace. `parseOpCommand` is deliberately shared by record-build.mjs's FENCE and gen-replay's PROJECTION: one grammar, so a command the projector could not read is denied mid-run rather than discovered after the run is paid for (epic #202 ticket #203)
  bus-toggles.mjs           Fieldwork's agentic-slot state commands (hand-written canon): the ONLY code in the repo that exercises the `agent.*` half of action-bus.mjs — every other emit is `ui.intent`, so the header's "adding a modality is a new `source`, not a new bus" was documented and nowhere demonstrated. mountSlotToggles emits `agent.set-tone` and a bus CONSUMER applies it to a deep-cloned working copy re-rendered through renderComposition (validate-before-DOM intact); action-bus.mjs is NOT edited and must not be — the point is that a consumer was all this needed. Refusals go to the aria-live readout, never a throw: action-bus.mjs:71-81 catches handler throws into console.error, which would hide the refusal from the reader AND trip proto-journey's no-page-errors contract. Each row filters on `target.id`'s slot part, so an action for the other slot is ignored rather than refused — the refusals it owns are an out-of-range index and a tone outside `TONES` on a MATCHING slot. Its `TONES` is a module-scope copy of the vocabulary's metric-tile tone enum, so proto-journey asserts it against the live vocabulary's enum (epic #164 ticket #176)
  device-frame.mjs          Verdant's resizable device frame (hand-written canon): pointer drag + WAI-ARIA window-splitter keys, clamped against MEASURED available width (at a 320px viewport the CSS default `94vw` already sits below FRAME_MIN, so a bare-constant clamp would widen the frame off-screen on the first keypress). REST GEOMETRY STAYS CSS — proto.css declares `width: var(--frame-w, min(392px, 94vw))` and this module writes `--frame-w` only once the reader acts, so nothing races the mount and the pixel gate captures what it always did (pack-boot.js's guaranteed-no-op discipline). A `pointermove` carrying no primary button ENDS the drag: firefox keeps delivering moves at clientX 0 once a captured pointer leaves the window, which snapped the frame to its minimum until proto-journey caught it. Sets [data-device-frame="ready"] in a `finally` on every path, including the no-phone early return (epic #164 ticket #176)
  studio-canvas.mjs + studio.css   the prototype studio's canvas SUBSTRATE (hand-written canon): a DOM stage holding real token-skinned components, panned by NATIVE SCROLL (so Tab's scroll-into-view keeps working — a transform-translate stage loses it), zoomed through a discrete level table, arranged by `data-col`/`data-row`. It therefore writes ZERO inline styles, which is what lets it join build-checks group 7 with no exception argued. CSS cannot import, so studio.css mirrors `MAX_COLS`/`MAX_ROWS`/`ZOOM_LEVELS` by hand and group 12 pins that mirror EXHAUSTIVELY and in both directions. The stage names NOTHING for a view transition (#171's lesson). Its rows are an EXPLICIT `grid-template-rows`, not `grid-auto-rows`: an implicit grid only materialises occupied rows, so the hit-test could not address the empty ones the keyboard path could — and that also makes `fit()` fit the whole canvas rather than the occupied part. #219 added the SPAN layer beside the caps it clamps against (`clampSpan` · `footprint` · `fits` · `MOVABLE` · `FRAME_CLASS`), `place()`'s one `kind: "frame"` branch and studio.css's FOURTH grid family (epic #202 tickets #204, #205, #219)
  studio-verbs.mjs          the canvas's MANIPULATION verbs (hand-written canon): move by pointer, by a click-move-click single-pointer path (SC 2.5.7) or entirely from the keyboard (SC 2.1.1 — two different criteria, and the header records which is which; the THIRD, SC 2.5.8's 24×24 target for `.stx-grab` itself, is recorded in studio.css beside the size, and says there that the Spacing exception was not relied on), plus undo/redo. THE BUS IS THE DRIVE PATH, NOT AN OBSERVER — both input paths emit one `ui.move` and do nothing else, and a single consumer applies it, so keyboard/pointer/agent parity is true by construction rather than by code paths that happen to agree, and #209's replay take-over has no seam to build. bus-toggles.mjs's pattern, second application; action-bus.mjs is NOT edited. A gesture is a PREVIEW and only the drop commits, which is what makes one gesture equal one action, one announcement and one history entry. An occupied cell is not enterable (no swap, no stacking). The history is seeded once at mount, so a component `place()`d AFTER it is ADOPTED into every entry — at PICK-UP as well as in the consumer, because a gesture previews before it commits and a consumer-only adopt would record a node's destination as its origin (#230; `place()` calling back into the verbs would be a circular import, which is why the fix lives here). Movement animates only on undo/redo, via `element.animate()` — which never touches `.style`, so group 7's regex and studio-journey's running-page assertion both stay literally true. Refusals go to the live region, never a throw. #219 widened it to the frames: `slots()` reads the exported `MOVABLE`, and every resolver gained span-awareness with a 1×1 DEFAULT — which is what lets build-checks group 13's existing cases run UNEDITED as the proof the widening preserved behaviour. `ui.resize` is a THIRD consumer and it argues itself against two recorded "NO BUS VERB, deliberately" refusals: the pointer and keyboard paths do not converge (a continuous drag and a stepped keypress), and undo needs ONE commit point in the one history. It shares the ONE `gesture` object via a `kind` field — twelve handlers key on that variable, so a sibling `resizing` state would be twelve silent conditions — and its keyboard model is this canvas's (Enter · arrows · Enter · Escape), NOT #176's commit-per-press, so one gesture stays one history entry (epic #202 tickets #205, #219)
  studio-flow.mjs           the flow's screen surface (#212, hand-written canon): renderScreen draws one place's screen — a heading (the focus target, tabindex -1), the composed components through renderComposition (ONE validation per screen, the refusal naming which screen), rule S4's honest empty sentence (imported from studio-export.mjs so the canvas and the exported file say it in the same words) and nav buttons counted from the board's connections; wireFlow makes navigation real — scroll the target wrapper into view, focus its heading, ONE fixed counted sentence ("<label>, screen k of N.") through the canvas's single live region. The SCREEN RULES live in pattern-rules.mjs's `screensFor` (the five patterns ARE the screen types: S1 the entry wears the board's own pattern, S2 a non-entry hub place is settings, S3 other worked places are queue, S4 a bare place is an honest empty screen) — the epic PRD's "patterns → flow screen types" question, answered inside the one-board model. NO BUS VERB, deliberately: pointer and keyboard converge natively on `click` and no agent path records navigation, so `ui.navigate` would be one emitter + one consumer invented for symmetry (the header records it; revisit when a replay records navigation). Navigation is CHROME, never a composed component — the three primitives are deliberately non-interactive, and a clickable list-row would change a shipped contract for every consumer. Mounted ONLY by studio-compile.mjs's applySwap, which is per-screen since #212: screens are 1:1 with wrappers BY CONSTRUCTION (the driver places one wrapper per place, no canvas verb adds or removes one, the ?b= restore requires equal lengths), which closed PR #235's L3 by DELETING the extra/surplus branches — a count mismatch now refuses loudly through a tripwire instead of misaligning silently. The pan-vs-click seam this surfaced lives in studio-canvas.mjs: a press on a real control never starts a pan, or capture eats the control's click (epic #202 ticket #212)
  replay-driver.mjs         the studio's REPLAY DRIVER (hand-written canon): plays a committed projection op by op over action-bus.mjs's reserved `agent.*` half — /factory's DEFAULT is `replay/build-fieldwork-dispatch.json` (the exported constants), and since #222 `mountReplay`'s `source` option `{ artifact, trace }` lets a page name its own pair, which is how the private instance plays its bespoke run (default-preserving: no `source`, byte-identical behavior) — the FIRST thing in the repo that exercises that direction for real — joined by `seq` with the run's own narration and its own fence refusals read from the committed curated trace, so the chrome shows the run's sentences rather than a generator's copy of them. **A second AUTHOR, never a second MOVER**: it changes the BOARD and reflects that onto the stage, and never emits `ui.move`, calls `applySlot` or writes `data-col`/`data-row` on an existing wrapper — where a block sits stays #205's sentence, and the day someone adds a mover here the parity AC #1 turns on is gone. **Only `place.add` calls `place()`**: studio-canvas.mjs:340 appends unconditionally even on the idempotent path and :343 announces on every call, so re-placing to re-label would re-order the stage AND turn four announcements into eleven — every other reflection renames the wrapper in place (studio-compile.mjs:382-388's call, inherited). The pacing is the run's own **proportionally, and the compression is STATED**: `PLAYBACK_MS` is the one reversibility seam (null plays the real gaps, and both branches ship), `paceBeats` keeps the gap RATIOS exactly, and the chrome prints the real duration and the computed factor — never "this is how long it took", which would not be true. Autoplays to completion on arrival behind a `finally`-set `[data-replay]`, so the settled canvas is the pixel baseline; hands over on the FIRST canvas interaction with no mode UI — captured on `canvas.scroll`, which is what makes excluding the zoom/verb/compile/transport rows structural rather than a heuristic, and Tab is deliberately not take-over. The committed artifact is ADD-ONLY (4 `place.add` · 7 `affordance.add` · 7 `connect`), so the rename/remove reflection branches are written but unexercised and build-checks group 16 pins the op histogram as a tripwire over them (epic #202 ticket #209)
  studio-method.mjs         the studio's METHOD BAND (#214, hand-written canon): the ten method questions as on-canvas cards, the assemblable Hook loop and the ethics verdict — a SECOND MOUNT of build-questions.mjs's answer store, never a second truth. Cards write through `setAnswers` (source "questions"), the redraft publishes through `publishBuild` (source "breadboard"), and the BUILD_CHANGE listener filters `"questions" | "restore"` (the #193 pattern, pinned as `RENDER_SOURCES`) — which is what makes the redraft loop-free, and what makes a `?b=` restore populate cards, diagram and verdict with zero interaction. An answer change WHOLESALE-redrafts the board via `draftBoard` and adopts it through studio.mjs's `adoptBoard` seam, which RELINQUISHES the replay driver first — the driver's post-settle seek would otherwise rebuild the run's board over the drafted one — and flips provenance on all three surfaces (notice · "This build" note · driver's provenance line), one DRAFTED constant, never three copies. Driver-gated exactly like the compile beat: disabled only while the replay will play, re-enabled synchronously inside `publishBoard` (settle writes `[data-replay="settled"]` and calls onSettle in the same task, so the pixel gate can never capture settled-but-disabled cards), never disabled on the declined `?b=` path. NO BUS VERB, deliberately: select-then-place converges pointer and keyboard natively on `click` (studio-flow.mjs's recorded no-symmetry-verb reasoning; revisit when a replay records method answers). The verdict unlock is UI-local completion state; its CONTENT always comes from the imported `quadrantFor`/`frequencyVerdictFor`/`QUADRANT_MEANINGS` — no second copy of any rule. Pure layer (`assembleReducer` · `hookComplete` · `verdictFor` · `RENDER_SOURCES`) gated by build-checks group 20; the running-page halves — gating, redraft-on-page, announcements, the restore — are tooling/studio-journey.mjs's methodPass (epic #202 ticket #214)
  studio-docs.mjs           the studio's DOCKED COMPONENT DOCS (#218, hand-written canon): MOUNT 2 of "Docs, two mounts of one source" — click or FOCUS a component on a compiled /factory canvas and `catalog.mjs`'s `renderComponentDocs` draws its generated docs in the inspector's fifth panel. It renders NOTHING itself: the renderer, the heading shift, the token re-resolve and the pack-swap observer are all imported, the join is `prepareHandoff(pack, vocab, graph)`, and the compaction is `system/catalog.css`'s `.cat-compact`. THE INDEX IS DERIVED — `className → row` from the generated pack, and the renderer's root class IS that class — never an extension of `pattern-render.mjs`'s `INSPECT_IDS` (hand-copied, three primitives, aborts the whole inspect activation on an unknown id; this one is generated, covers everything, and fails as one dead click). LAZY BY DISCRIMINATOR: nothing is fetched until the stage holds a `.stf-screen` (`COMPILED_SELECTOR`, exported so the gate pins the string the module branches on) — at rest the page has issued no request for this panel, and NO other gate in this repo can see that, which is why the rule is the pure `shouldLoad` and the join is the pure `loadDocsModel`. DELEGATION on `canvas.stage`, never per-node listeners (a compile, a revert, a `?b=` restore and a redraft all replace the nodes). NO ANNOUNCEMENT, deliberately — `focusin` fires on every Tab step, so the affordance is stated ONCE as a static caption every trigger `aria-describedby`s (`studio-verbs.mjs`'s `#stx-move-help` idiom); NO BUS VERB, deliberately (`studio-flow.mjs`'s reasoning, third application). `inspector.activate(i, false)` is LOAD-BEARING: `true` would yank focus out of the canvas on every Tab press. It accepts the double-open with the inspect bubble as a decision, not a side effect (`tabindex="0"` makes the three primitives focusable, which `applySwap`'s `data-inspect` had never been). Refusals are content in the panel; no self-boot — `studio.mjs` mounts it and calls `refresh()` wherever it calls `syncInspect()`. Gates = build-checks group 23 (the rules) + `tooling/studio-journey.mjs`'s `docsPass` (the wiring) — and each states what the other owns (epic #202 ticket #218)
  studio-frames.mjs         the studio's DEVICE FRAMES (#219, hand-written canon): the two shipped prototypes — Verdant's phone screen and Fieldwork's dispatch board — as real `<iframe>`s on the /factory canvas, arranged by the same `data-col`/`data-row` grammar, moved by the same handle and the same `ui.move`, resized by a new `ui.resize`. NO PROTO FILE IS EDITED, and that is the design working rather than luck: both pages already gate the dock, the inspect layer, the palette and the standalone device frame on `window.self === window.top` (#175, #176), so an embed gets the pack skin and no nested chrome for free — asserted on the frame's own `contentDocument`, which the pixel gate structurally cannot do. THE FOURTH GRID FAMILY, not a fifth `.stx-slot`: that class means BOARD WRAPPER, and `studio-compile.mjs`'s two tripwires, `studio.mjs`'s `arrangementNow()` (#208's `g`) and `adoptBoard`'s removal loop all keep meaning it — so a frame SURVIVES a redraft and a compile, and `.stx-guide`/`.stx-menu` (#217) had already opened this door. It draws no wrapper: `studio-canvas.mjs`'s `place()` gained ONE `kind: "frame"` branch, so six argued-for rules (idempotency, handle-first tab order, born-inert handles, the #231 L3 re-label, the id counter, the `say()`) are inherited rather than copied. GEOMETRY IS ATTRIBUTES AND RESIZE IS SPAN, NOT PIXELS — forced, not chosen: build-checks group 7 asserts `writes === 1` across every studio module and this is one, so #176's px `--frame-w` is unavailable and `data-span-col`/`data-span-row` select `grid-*-end: span K` rules from `studio.css`. The trade is that resize is STEPPED, which is also what makes it announceable. The HEIGHT deliberately does not ride `--stx-slot-h` (`studio.css:638` flips that to 480px once a board compiles, and a depicted phone that grows when you compile is a lie about the device), so `--stx-frame-unit` is a hand-mirror group 12 pins. A FRAME IS OUTSIDE #217's SELECTION LAYER, stated rather than omitted: half-widening is a bug factory (`chosenNodes()` is `.stx-slot[data-stx-selected]` while `slots()` is now `MOVABLE`, so a marquee that could MARK a frame the group move never RETURNS would drop it silently). What IS ready for the day that changes: `groupDelta`/`groupStep` are footprint-aware, because `preview()` routes every gesture — single-node ones included — through `groupDelta`. What is NOT: `groupOccupancy`, named in both headers as the remaining piece. It takes NO BUS and emits nothing — every action on a frame is `studio-verbs.mjs`'s. A DROPPED BRAND DOES NOT REACH THE FRAMES (custom properties do not cross a document boundary) and the caption says so to the reader; copying the vetted token map in would make the one-application-point invariant `writes === 1` become 2. Gates = build-checks group 24 (the descriptors + `packHref`) + `tooling/studio-journey.mjs`'s `framesPass` (the rendering, the nested-chrome absence, the pack swap, the three-source resize parity, the mixed-verb undo) — and each states what the other owns (epic #202 ticket #219)
  catalog.css               the catalog's surface styles, linked wherever renderComponentDocs mounts (#218: components.html + factory.html; #222: instance.html): `components.html`'s page `<style>` moved verbatim, so the `.cat-*`/`.hv-*` rules — and the `[hidden] { display: none !important }` rule the code-tab panels depend on (the /build #138 lesson) — travel with the RENDERER rather than with one page. Not `components.css`: that is the shipped system every page and both protos load, and these are docs-surface styles. `.cat-compact` is the one block #218 added, and it carries the type sizes too — `opts.level` shifts the TAGS and a tag carries no size, so without them the rail would still render at h2 scale
  loc-summary.json          GENERATED (agent-layer/gen-loc-summary.mjs, drift-checked): measured file/line counts of the shipped groups, lines rounded to the nearest 100; approach.html renders its numbers — a size claim is never hand-written
  param-manifest.json       hand-maintained manifest of every live-manipulable control on the 11 shipped pages + chrome (one entry per control; counting rules in its $description) — the one hand-written input to gen-param-count (epic #164 ticket #167)
  param-count.json          GENERATED (agent-layer/gen-param-count.mjs, drift-checked): per-page + site-wide control totals counted from param-manifest.json, exact not rounded; approach.html renders the total — a capacity claim is never hand-written
  system-graph.mjs          view-time "shape of the system" exhibit (hand-written canon): prepareGraph + renderSystemGraph draw the generated token↔consumer graph as factory.html's #shape section — edges on hover/focus only, pack values verbatim (portfolio-ux-uplift §Phase 5). #173 added the bounded pan/zoom window: drag-to-scroll on a native scroller (Tab order survives), ⌘/Ctrl-wheel + trackpad pinch, explicit in/out/reset buttons as the keyboard path, one SVG width write against the unchanged viewBox — at rest scale 1 / scroll 0,0, so the hidden-at-capture panel never races the pixel gate
  system-graph.json         GENERATED (agent-layer/gen-system-graph.mjs, drift-checked): contract tokens × components.css consumer blocks × the three packs' raw bindings — the graph is measured, never hand-drawn
  derivation-roundtrip.mjs  view-time Verdant derivation round-trip exhibit (hand-written canon): prepareDiff + renderRoundTrip render #40's committed fidelity diff (proposed-vs-truth ΔE · human gate · verdict · 12 WCAG pairs) beside the mounted pack-seed-verdant trace — static honesty framing + JS-rendered numbers, fetches committed files only; designed surface = Factory page #round-trip exhibit (epic #38 ticket #42)
  instance.mjs              view-time private-instance shell config (hand-written canon): reads window.INSTANCE_CONFIG → fetches a scenario package → honesty notices (fictional + speculative + sources) · curated intake with reasoning · the SHARED wizard via factory-intake.mjs's initIntake(config) seam (configured, never forked) · embedded pack-seed trace · THE STUDIO BAND (#222): one mountStudio(document, { replay: INSTANCE_CONFIG.replay }) call into studio.mjs, whose self-boot stands down on the shell's data-studio-mount="external" — the same modules /factory mounts play this instance's OWN recorded build run; a missing/malformed `replay` renders an honest error card and mounts NOTHING, because falling back to the public demo run on a "built for you" page would be the dishonest degradation · the handoff link slot; driven by instance.html — the variant a real application deploys unlisted (epic #38 ticket #43; studio re-shell epic #202 ticket #222, which retired agentic-study.mjs and #89's composed-view slot)
  ── the /build chain (epic #134): the visitor's OWN tokens + the visitor's OWN product. Five mounted
     entry points + three pure modules, all hand-written canon and all committed-rules-in-the-browser
     — nothing uploads, no model is called ──
  build-import.mjs          Act 0: reads a dropped .json token export IN THE BROWSER (DTCG · Tokens Studio · any nested name→value), maps it onto the contract by ROLE, and dresses BOTH stages; 32 MB cap; the report states whose design work it is before it says anything else (ticket #135)
  build-questions.mjs       the shared answer store + the BUILD_CHANGE contract + both acts' stepped wizards (Hooked ×7, Shape Up ×3) + the verdict panel (Manipulation Matrix quadrant · frequency filter), one mount per act over one answer set (ticket #136)
  breadboard.mjs            Act 3: Shape Up's breadboard, drafted from the ten answers and then editable — rename · add · remove · re-point, every verb announced in one live region and every verb placing focus; LABEL_MAX is the cap both the inputs and the codec share (ticket #136)
  pattern-rules.mjs         the three committed rules that name a pattern from a board (1 the `shape` answer names it, 2 the hub override reads the BOARD and outranks the answer, 3 an empty board names none) + the slots COUNTED from the board, never invented, for ALL FIVE patterns since #139. `SLOT_MAX` truncates exactly one of them (feed reads the whole board, so 36 candidates can become 6) and `affordanceCount` is how the surface states that (tickets #137, #139)
  pattern-render.mjs        Act 4: renders the named pattern through the site's own vocabulary-validated agentic-renderer — dashboard→metric-tile, queue/feed/settings→list-row, onboarding→sequence-step; feed and settings share a primitive because their derived slot IS list-row's contract, and what differs is arrangement (build.html's .bx-pat-slots.is-*). The out-of-library refusal card is RETAINED and currently unexercised: it is the contract a SIXTH pattern gets, not a fake. Sets [data-pattern-stage="ready"] only after an IntersectionObserver-gated vocabulary fetch — a VR waitVisible handle, never waitReady (tickets #137, #139)
  build-card.mjs            the build's SVG artifacts (build card + breadboard), escaped once, hostile labels and tokens neutralised at the template (ticket #137)
  build-share.mjs           the whole build in the URL: deflate-or-raw codec + the tamper battery's counterpart — every payload validated whole, caps enforced, bidi controls and magnitude bombs rejected (tickets #137, #138)
  build-keep.mjs            Act 5: the keep rail — card, four downloads, the share link, and the ?b= restore; hides all three artifact tiers when the board is bare (ticket #137)
  pack-import.mjs           the token-import ENGINE, ONE never a fork: a design's token export → a contract pack, role-mapped and contrast-negotiated. The Figma CLI, the portal drawer, home's drop zone and /build's Act 0 all map through THIS file, or a browser would map a design differently from the CLI that produced the committed packs (ticket #130)
  brand-import.mjs          HOME's public drop-to-re-skin surface (beat 02): the drop zone, the mapping report with its WCAG receipts, "wear it" across the visit, and a tokens.<slug>.css download byte-identical to the CLI's (ticket #130)
  pack-imported.mjs         the imported-pack RECORD (sessionStorage, sibling of pack-derived.mjs) + `vetTokens` — the allowlist every visitor-supplied value passes through before it reaches the DOM, on home and on /build alike; tooling/build-checks.mjs's vetting group guards it (ticket #130)
  tokens.css / tokens.saulera.css / client.config.js   real packs kept as reference; NOT loaded by the shell
  specs/                    ComponentSpec .md + DataContract .json — the handoff source of truth (format: .claude/references/kb-format.md)
  wc/                       custom-element wrappers (vd-*) + demo.html — hand-written canon, spec-first, shadow CSS reads only spec-head tokens (epic #1 ticket #12)
  figma-import.md           DTCG→Figma import-path doc; ships in the pack via gen-handoff
index.html + approach/factory/work/contact.html + 404.html   the shipped five-page IA on the neutral shell — Home is the 90-second recruiter gate; factory.html reserves the deep-linkable route as an honest stub (epic #1 ticket #6)
build.html                  the pattern builder — the sixth public surface, and the only one about the READER's product: their token export (or one colour) → the ten method questions → an editable breadboard → a real UI pattern under their brand → a link that rebuilds the whole build. OFF-nav and `noindex`, linked from the home close card, work.html's "Run it" grid and the footer site index (#148 — the footer claims to be the full index, so it had to be; one chrome item churns all 16 chrome-bearing baselines, the two proto pages carrying none). Six acts over five module entry points (build-import · build-questions · breadboard · pattern-render · build-keep), which pull in the three pure ones below; no upload and no model call (epic #134, tickets #135–#138, #148)
components.html             the component catalog — off-nav, `noindex`, in the footer site index and the VR page set: every vocabulary component as an anchor-addressed section (hash deep links survive reload — the mount re-runs the scroll+focus the browser's native hash pass missed), rendered by system/catalog.mjs from the generated artifacts alone. In the ⌘K palette (a static "Go to Components" + one command per component). Its `.cat-*` styles are NOT in a page `<style>` block any more — #218 moved them to `system/catalog.css`, which this page and `factory.html` both link (epic #202 tickets #215, #218)
studio.html                 the prototype studio's off-nav, `noindex` HARNESS — drives studio-canvas.mjs + studio-verbs.mjs RAW over ~31 real components, so the substrate can be driven and gated before anything designed depends on it. Not in the five-page IA, not in the footer index, not in the VR page set, and it carries no `param-manifest.json` entries for the same reason (the manifest scopes itself to the VR-gated pages + chrome). The designed surface is /factory, and that route surgery is #206 (epic #202 tickets #204, #205)
instance.html               the deep-link-only private-instance shell (off-nav, not in the five-page IA): a real application deploys it unlisted; demo-configured to scenarios/northwind, its #beat-built band re-shelled onto THE STUDIO at #222 (a data-studio-mount="external" shell instance.mjs boots with the instance's own recorded run; configured by DOM omission — no [data-studio-frames], only the this-build/shape/component-docs inspector panels, the keep rail in-band). Carries build-instance.mjs's stamping seams — INSTANCE_CONFIG markers, data-when="demo"/"real" prose regions, the chrome-config script anchor — that #44/#222 rewrite demo→real per company. Every non-fragment BODY link is deploy-safe by construction (mailto/GitHub/in-dir — #160's lesson): a public-site route may exist only inside a data-when="demo" region, which stamping deletes (epic #38 tickets #43, #44; epic #202 ticket #222)
_headers                    security headers + noindex (revisit at launch — epic open question)
agent-layer/                build-time Node ESM generators: machine-readable projection of one site
  build.mjs                 orchestrator — run FROM THE JOBS FOLDER against a decisions ledger
  build-instance.mjs        SIBLING orchestrator (NOT a gen-*, not in build.mjs): compiles one company brief (gen-company-package) + a derived pack + a trace + a RECORDED BUILD RUN (`--replay <slug>`, REQUIRED — the run's five files copied from the repo tree at their own root-absolute paths; `--trace`'s copy-not-run discipline; a company-real run is recorded into the WORKING TREE and never committed, `git clean` after building) + the #43 instance.html shell into a self-contained deploy dir OUTSIDE the repo, stamps the shell demo→real, GENERATES the per-instance chrome config (system/client.instance.config.js — every link in-dir or mailto/GitHub, which closed #160), always ships the studio's docs chain (handoff/verdant/pack.json + vocabulary.json), emits a standalone noindex _headers, and PRINTS the wrangler direct-upload command — deploy stays a human step; nothing company-real is committed. validateAssembly v2 refuses ANY reference that does not resolve in the deploy dir (`auditRefs` — mailto/https exempt; the old chrome exclusion is gone); the pure half (exported `stampShell` + `auditRefs`) is build-checks group 25's, the RUNNING built page is tooling/instance-journey.mjs's. v2 retired `--compositions`/`--proto` with the composed-view slot they fed (epic #38 ticket #44, folds spike 2; v2 epic #202 ticket #222)
  lib.mjs                   ledger parser + shared helpers
  gen-replay.mjs            GENERATED + drift-checked: projects a committed build run into `replay/<slug>.json` — the ops a driver plays, `{op, atMs, phase, params, fromStep}`, with `atMs` the run's REAL pacing and every `source` value copied from the curated meta. Its `projectTrace` is PURE so build-checks group 11 can drive it. Three things a future editor must know: it projects from the CURATED trace, which works ONLY because `tooling/curate-trace.mjs:17` keeps `command` in `KEEP_WHOLE` (every other input string clips at 700 chars); the REPRODUCE CHECK re-applies the projected ops and demands they rebuild the committed board exactly, which is what makes the projection a claim about the run rather than about itself; and discovery runs in BOTH directions, so an orphaned `replay/<slug>.json` with no board behind it is an error rather than an invisible stale file (epic #202 ticket #203)
  gen-*.mjs + inject-jsonld.mjs   one file per emitted artifact (canon token CSS, decisions.json, DTCG tokens.json, llms.txt, _headers, JSON-LD); each runnable standalone
portal/                     local-first workbench (127.0.0.1 only, never deployed)
  server.mjs                zero-dep node:http server — thin route dispatch, logic in lib/
  lib/                      one concern per module: env.mjs (paths + .env), kb.mjs (card projections), intake.mjs, chat.mjs (Agent SDK behind SSE), trace-recorder.mjs (Agent SDK hooks → Trace JSONL), origin.mjs (the CSRF guard `server.mjs` applies before ANY routing: binding 127.0.0.1 stops a remote client but not a page in the operator's own browser, and `readBody` JSON.parses regardless of content-type, so a no-preflight cross-origin POST would otherwise reach a token-spending run. Accepts BOTH `http://localhost:<PORT>` and `http://127.0.0.1:<PORT>` — reject one and the drawer, intake form and chat pane break for whichever the operator typed — and an ABSENT Origin, which is every curl and every same-origin GET. build-checks group 9 gates the predicate; the WIRING is only ever proven against a running portal, since `server.mjs` reaches the SDK) (#157, deferred from PR #156's review), builder.mjs (the OPERATOR PATH: /build's ten answers → the question a real composition run answers — three committed rules (`shape` names it, `action` names who for, the other eight enter no prompt), the `fictional:false` privacy refusal, the run lock and the SSE whitelist; imports the question config from system/build-questions.mjs, never forks it, and its whole graph is SDK-free so tooling/build-checks.mjs can gate it in CI) (epic #134 ticket #140)
  public/                   vanilla SPA — hash routing, template strings, no framework
  record-trace.mjs          build-time trace recorder run (CLI, not deployed): wraps the Agent SDK in hooks, emits traces/<slug>.raw.jsonl — a REAL agent run (epic #1 ticket #5)
  record-composition.mjs    build-time composition runner (CLI, not deployed): a REAL Agent SDK run composes a {name,props,children} dashboard view for one question about one SCENARIO → proto/compositions/<scenario>/<slug>.json + a scenario-scoped index.json + a PIV trace pair; prompt built ONLY from vocabulary + the scenario's declared fixtures + question + slot bounds (no example); the scenario is parameterized via scenarios/<slug>/compose.json (subject/today/fixtures/copy/slots/computeRules[DEFINITIONS-ONLY]/questions — the Read fence is rebuilt from it), so the runner has no baked-in scenario; reuses recordRun + validateComposition + curateTrace; keep-gate is validateTrace (epic #1 ticket #13, folds spike 6; parameterized epic #86 ticket #88)
  record-build.mjs          build-time INCREMENTAL build recorder (CLI, not deployed) — record-composition.mjs's sibling, and diffable against it: a REAL Agent SDK run reads one human-authored brief and assembles a Shape Up breadboard ONE OP PER TOOL CALL, so one call is one op is one trace step (a composition run's implement act is a single Write, which projects to a cut rather than a build). The fence is tighter in the one direction that matters: Write/Edit DENIED OUTRIGHT, Read = the brief only, Bash = only what `system/board-ops.mjs:parseOpCommand` accepts, running THE build tool (resolved as a path IDENTITY — a filename suffix is not one) against THIS run's board. **A build run's `artifacts` count is therefore 0 BY DESIGN** — the Trace format's Write→artifact pairing never fires, and nothing in the trace links it to `replay/<slug>.board.json`; that link is enforced by gen-replay's reproduce check instead, deliberately, because un-denying Write to recover the format-level pairing would let the agent write anything to that path on the run whose whole subject is fence discipline. Same lazy-SDK import, same `--dry`/real split, same drop-on-failure ship gate (epic #202 ticket #203)
scenarios/                  scenario packages — the demo subjects + fixtures (format: scenarios/README.md; validate: node scenarios/validate.mjs; check page: /scenarios/check.html)
worker/                     fixture-backed mock API — public read-only GET, one Cloudflare Worker (cd worker && npx wrangler dev); site degrades to static fixtures via system/scenario-data.mjs
proto/                      data-connected prototype pages — Verdant phone screen + Fieldwork hybrid canvas (vd-/fw- components implemented to system/specs/); Fieldwork's two agentic slots render #13's canonical proposals. Since #175 both wear the site's pack skin (pack-boot + a top-window-only dock) and carry inspect mounts — and because their components render from an AWAITED fetch, each re-inits inspect.mjs after its innerHTML lands, or a reader with inspect persisted on gets a screen wired to nothing. Since #176 both are also MANIPULABLE rather than view-only: Verdant's bezel is a draggable/keyboard-resizable frame whose screen reflows through `@container` queries (the queries live in proto.css, never components.css — a rule there would move system-graph.json → inspect-data.json → factory's baseline), and each of Fieldwork's FILLED slots carries a control row commanding its tiles over the action bus. Both mounts are top-window only, so work.html's iframe embeds stay bare; both set a VR readiness handle in a `finally` ([data-device-frame], [data-bus-toggles]) because both paint at-rest chrome that arrives after load
proto/compositions/         COMMITTED composition proposals (one {name,props,children} JSON per question) + index.json manifest — generated by real record-composition.mjs runs; consumed by the Fieldwork slots (the study page that also rendered them retired at #222 — the proposals stay as committed real-run records) (epic #1 ticket #13)
traces/                     committed real agent-run traces (format: traces/README.md): raw + curated JSONL pairs from portal/record-trace.mjs (+ record-composition.mjs for #13's composition runs, + record-build.mjs for #203's build runs); validate: node tooling/validate-trace.mjs; replayed by system/trace-player.mjs
replay/                     the studio's replay artifacts (format: replay/README.md) — and the rule the whole tree exists to state: **a replay artifact is a PROJECTION of a committed real run, never a recording.** The recording is the trace pair in `traces/`, committed beside it under the standard labels and named in the artifact's `source`; when the two disagree the trace is the truth and the artifact is regenerated, never the other way round. Three files per run: `briefs/<slug>.md` (human-authored INPUT — a person writing the problem statement is not the honesty contract's subject, and the brief states the problem, never the answer), `<slug>.board.json` (the run's real OUTPUT, what the ops built) and `<slug>.json` (GENERATED by gen-replay.mjs, drift-checked). Never hand-write or hand-edit any of the three (epic #202 ticket #203)
docs/epics/                 PRD + architecture decisions governing the platform build
docs/figma-runbook.md       operator steps for the Figma boundary: import a design as a pack (once per design, one command) vs the parity round-trip (once, ever) + the request-budget rules
handoff/                    GENERATED handoff pack (verdant/) — committed, never edited by hand; regenerate: node agent-layer/gen-handoff.mjs (pack + tokens + wc wrappers) · node agent-layer/gen-vocabulary.mjs (vocabulary.json — the agent-ready layer the renderer validates against) · node agent-layer/gen-pack-bundle.mjs (pack.bundle.json — every pack file inlined as one downloadable bundle, the /handoff.html download); figma-parity.json is the one exception: written only by the parity script's real run
tooling/figma/              the Figma boundary — secret-gated (portal/.env), standalone-only, never registered in build.mjs
  figma-read.mjs            the SHARED read both scripts use: auth, the Enterprise variables gate, the paged fallback, and the response cache that conserves a Starter file's ~6 GET-file/month (#110). `--from <export.json>` skips the API entirely — a plugin export has no rate limit AND sees the variables REST gates behind Enterprise
  figma-parity.mjs          READ-BACK: diffs a Figma file against the token contract → handoff/verdant/figma-parity.json
  figma-pull.mjs            IMPORT: a Figma file's colour ramps → system/tokens.<slug>.css, mapped by ROLE (ramps share no names with the contract), contrast-negotiated within the file's own ramps against RULESET.wcagPairs — the same list derive() is held to
tooling/mcp/                local MCP helper scripts
tooling/style-dictionary/   a dependency-carrying tool; emits the pack's multi-target tokens (css/ios/android)
tooling/visual-regression/  isolated Playwright (the other dependency-carrying tool); CI visual-regression gate — screenshots the 11 shipped pages (6 IA + /roundtrip + /build + /components + 2 proto) under neutral + saulera packs, pixel-diffs vs committed Linux baselines. Capture = measure → resize → wait for any activateOn:'visible' beat → RE-measure until the height stops moving; skip that last step and the shot silently truncates whatever the beat added (#9 gate 3/3; /build + the re-measure fix #138)
tooling/wc-sandbox/         React 19 harness for the wc wrappers (esm.sh import map, no install)
tooling/curate-trace.mjs · validate-trace.mjs   deterministic trace curation (selection + truncation only, rules recorded in meta) + the Trace format's drift guard (candidate CI gate #9)
tooling/board-op.mjs        the fenced build agent's ONLY build tool (epic #202 ticket #203): `node tooling/board-op.mjs <board.json> '<op json>'` applies exactly ONE op and prints the whole resulting board back, ids included — so one Bash call is one op is one trace step, and the agent learns the machine-assigned ids from real output instead of inventing them. `--validate` is its assertBoard form. It runs the SAME `parseOpCommand` the fence denies with, so there is no third opinion about what an op call looks like. It writes wherever it is pointed on purpose: the RECORDER'S FENCE, not this file, constrains the target — a second path allowlist here would be a second copy of the fence, and it would drift from it
tooling/build-checks.mjs · build-journey.mjs   /build's two gates. `build-checks` is PURE (imports the shipped modules, opens no browser) and RUNS IN CI (verify job): 25 groups — pattern rules, slots, compositions against the real generated vocabulary, the share codec + its tamper battery, SVG escaping, the one-application-point vetting invariant, group 8 the OPERATOR PATH (`portal/lib/builder.mjs`: rule coverage driven by the shipped config, rule 3 proven by changing each of the other eight answers and asserting the drafted question is byte-identical, every guard called directly, the stepEvent whitelist, the run lock), and group 9 the ORIGIN predicate (`portal/lib/origin.mjs`: both loopback origins accepted, an absent Origin allowed, 16 near-misses refused, the port a parameter — the PREDICATE only; the guard's wiring can't be reached from CI), and group 10 the BUILD ANALYTICS contract (`system/analytics.mjs`: both `/build` virtual paths static, no `?b=` payload in either, each fires once, and the real URL restored verbatim across two OVERLAPPING flips in both orderings), and group 11 the REPLAY PROJECTION + THE RECORDER'S FENCE (`agent-layer/gen-replay.mjs`'s pure `projectTrace` over synthetic in-memory rows: the happy path plus the corrupted-label MUTATION that decides whether the reproduce check is real rather than a tamper check, five refusals each naming its `seq`, `--validate` and failed calls proven not to be ops, `atMs` proven to be real pacing rather than an index, the honest label, and the `KEEP_WHOLE` coupling proven by RUNNING `curateTrace` over a >700-char command — never by grepping for the constant, since it and `truncateInput` are both module-private; plus case 8, which drives BOTH callers of the shared grammar — `projectTrace` and `portal/record-build.mjs`'s exported `makeFence` — over a decoy script path (`/tmp/evil/tooling/board-op.mjs`) and asserts that both forms a real run legitimately types stay accepted, relative and absolute, because tightening the identity inside `parseOpCommand` instead would silently kill `--dry`. Importing the recorder here is safe ONLY because its SDK import is lazy, so this group rides on group 8's SDK-free invariant too; the fence's WIRING — that this predicate is what `canUseTool` receives — stays unprovable in CI, the same split group 9 lives with), and group 12 the STUDIO CANVAS (`system/studio-canvas.mjs`: studio.css's hand-mirror of the caps and the zoom table, pinned exhaustively and in BOTH directions rather than by a count — "there are MAX_COLS [data-col] rules" passes happily for a set with a duplicate and a gap — plus `clampSlot` over 8 hostile slots and `fitLevel`'s snap-down, floor, cap and zero-dimension answers), and group 13 the CANVAS VERBS (`system/studio-verbs.mjs`'s pure half: the history stack including #230's `adopt` — which teaches EVERY entry an id placed after mount, fills MISSING ids only (that is what lets the mount's two call sites compose) and stays inert otherwise — plus `stepSlot` and `hitSlot`. Like groups 9 and 11 it STATES the boundary it cannot reach — the single-consumer invariant AC #1 turns on is a running-page fact, and its owner is `tooling/studio-journey.mjs`'s three-source proof. Its deep-compare is a hand-written recursive canonical stringify, NOT `JSON.stringify(v, keys)`: an array in stringify's second position is a REPLACER and filters property names at every level, which made every comparison in the group vacuous until a mutation sweep caught it)., and group 16 the REPLAY DRIVER's pure layer (`system/replay-driver.mjs` driven over the REAL committed artifact + trace: the join's counts asserted against the FILES rather than typed, the reproduce claim restated at view time — playing every op beat rebuilds `replay/<slug>.board.json` exactly — with the corrupted-label MUTATION that decides whether that compare is real, the schedule's gap RATIOS proven against the run's own `atMs` so an index-derived pace fails, both `PLAYBACK_MS` branches, the chrome's strings asserted by IDENTITY against the parsed files, totality over 10 junk pairs, and the artifact's ADD-ONLY op histogram pinned as a tripwire over the unexercised rename/remove branches. Like 9, 11 and 13 it STATES its boundary: the `agent.*`-only emission, the single consumer, the announcements, the settled DOM's determinism and the take-over discriminator are all `tooling/studio-journey.mjs`'s), and group 18 the DOCS CHAIN (#211 — `agent-layer/gen-vocabulary.mjs`'s pure `validateExamples` and `system/handoff-viewer.mjs`'s `prepareHandoff`: every committed spec's `example` proven to RENDER against the real vocabulary, the count read from `pack.json` rather than typed, plus the MUTATION that decides whether the gate can fail at all — four synthetic broken examples, one per refusal branch, each asserted to throw AND to name its own spec path; then the view-time join over the real `pack.json` + `vocabulary.json` + `system-graph.json`, whose `consumer` assertion is anchored on the PACK deliberately, since a graph-derived expected set moves in lockstep with the thing under test and can never go red. Like 9, 11, 13 and 16 it STATES its boundary: that a catalog RENDERS any of this is #215's, and there is no catalog yet), and group 19 the FLOW (#212 — `pattern-rules.mjs`'s `screensFor` over the REAL committed replay board: reachability from the entry, nav counted from `connections` and nothing else, the 1-dashboard + 3-queue type mix pinned as a tripwire; a flow fixture per in-library screen type via the BOARD_FOR rule, rules S1–S4 each proven to fire by their own reason sentences, feed's truncation stated by `streamNote` IDENTITY on the canvas and in the exported file, with the per-place bound proven un-truncating, the empty board null at every layer, every composed screen validated against the real vocabulary, two mutations — a tampered export fails href resolution, a re-typed screen fails the histogram — and totality; the click, the focus, the announcement and the cold `file://` open are studio-journey's flow pass and the manual check's), and group 21 the CATALOG's pure layer (#215 — `system/catalog.mjs`: pack↔vocabulary set identity, palette's static `CATALOG_COMPONENTS` pinned against the vocabulary, `controlFor`'s bounds fidelity with hasOwn asserted BOTH ways so an invented bound fails, the 3/17 wrapper histogram as the next wrapper's tripwire, `WRAPPER_ATTRS` pinned against the wrapper sources' text-parsed observedAttributes + the vocabulary with the type:"type" mutation, `reactSnippet`'s projection + escaping, and a committed spec file behind every copy button; the running page is `tooling/catalog-journey.mjs`'s — count from the fetched artifact, deep links, live re-serialization, byte-identical copy, the held-route ⌘K race, the 3/17 gating with the paste-and-render proof, refusal-as-content, the bus readout from pointer AND keyboard, ×3 engines, operator-run), and group 23 the STUDIO'S DOCKED DOCS (#218 — `system/studio-docs.mjs`: `docsIndex` over the real pack with the collision throw and the class-rename MUTATION; every pack class asserted to be a class `agentic-renderer.mjs` ACTUALLY EMITS (source-text, three template forms) plus every component the committed replay board really compiles proven to be a doc trigger — the assertion that reddens the day a class, spec name or template class drifts and the canvas silently stops being clickable; `headingTags`; and the TWO INVARIANTS no other gate in this repo can see — `loadDocsModel` driven with a stub fetch, asserting exactly `DOCS_SOURCES` was asked for and that the THIRD argument's fields survived, with the graph-omitted mutation that decides whether that can fail (a two-arg regression renders a plausible panel with `tokens`/`example`/`wrapper`/`consumer` silently null), and `shouldLoad`'s full truth table WITH a positive control, plus `COMPILED_SELECTOR` pinned against `studio-flow.mjs`'s own `renderScreen` class. Like 9, 11, 13, 16, 18 and 19 it STATES its boundary: the click, the opened panel, the cross-page agreement, the un-stolen focus, the live token values and the at-rest request count are `docsPass`'s), and **group 24** the STUDIO'S DEVICE FRAMES (#219 — `system/studio-frames.mjs`: `FRAMES` frozen at BOTH levels by mutation, every `src` and `standalone` proven to be a real committed file with the `/proto/nope.html` mutation that decides whether that check can fail — which matters precisely because the pixel gate MASKS this content, so a 404 inside a frame compares cleanly against its own baseline forever — every caption proven to carry the site-pack sentence that discharges the dropped-brand decision, both footprints on the grid by `clampSpan`'s OWN definition, disjoint cell by cell through `footprint()` and clear of ROW 1 with `arrangeBoard` named as the reason, and `packHref` over a stub document including the CONTRACT-line trap and totality over 6 junk documents, because an unloaded frame's `contentDocument` is one. Like the others it STATES its boundary: the rendering, the absent nested chrome, the mid-visit pack swap, the three-source resize parity and the ready handle are `framesPass`'s), and **group 25** the INSTANCE STAMP + CHROME AUDIT (#222 — `agent-layer/build-instance.mjs`'s exported `stampShell` driven over the REAL committed instance.html with a synthetic company: zero data-when=/{{/stray-hidden residue with the [data-studio-notice] at-rest notice as the ONE sanctioned standalone hidden, body text free of the demo name/"demo"/"fictional" with the company name proven substituted, the v2 INSTANCE_CONFIG parsing as pure JSON with `replay` in and `composition`/`links.prototype` proven ABSENT, the chrome-config <script> re-pointed (#160) and the pack <link> stamped; MUTATIONS: an extra data-when="demo" region still stripped, a missing #instance-name anchor AND a missing chrome-config anchor each thrown naming the anchor, and `auditRefs` refusing /approach by name while accepting mailto/https/fragments/in-dir and refusing http/protocol-relative/bare-relative/javascript:/empty. The RUNNING built page is `tooling/instance-journey.mjs`'s, operator-run, and this group states it cannot reach it). Group 8 is also where the SDK-free invariant is proven, and it is proven by CI's ABSENCE of `portal/node_modules` — locally, check it with `mv portal/node_modules portal/node_modules.off && node tooling/build-checks.mjs; mv portal/node_modules.off portal/node_modules`. `build-journey` DRIVES the page across chromium + firefox + webkit (Playwright resolved out of tooling/visual-regression/node_modules, never a repo dep) — its assertions cover the share round-trip, the tamper refusals, an emptied board, a 33 MB refusal, reduced-motion, the dock mid-flow, and that all three links in resolve. Operator-run, not in CI (epic #134, tickets #137, #138, #148)
tooling/vt-verify.mjs        /build's THIRD driver, and the one the other two structurally cannot be: it asserts that the view-transition morphs actually OPEN. `build-checks` and `build-journey` only ever assert END STATES, and `system/morph.mjs` falls through to a plain mutation whenever it cannot transition — so a morph that silently stopped firing leaves every other assertion green. It wraps `document.startViewTransition` before any module evaluates and reads `document.getAnimations()` for the running `::view-transition-*` pseudos, per engine: /build's boot opens ZERO transitions (the property the pixel gate depends on — the gate never interacts, so at-rest capture can never race a morph), each of the three families opens exactly one with the group names we wrote (`bx-q-<act>` · `bb-place-<id>` · `bx-pattern` — a duplicate name would abort the whole transition silently, and shows up here as a MISSING group), a rename opens none, and reduced motion opens none while the interaction still completes. #172 extended it past /build to the five other wrapped surfaces (home + instance wizard steps, the study's tab and remove verbs, a trace step), which name nothing and so assert the count and the end state rather than group names. Load is an EXPECTED COUNT there, not an assumed zero: home opens TWO at load and they are not a morph wrapper's — `spine.mjs`:147,149 runs the hero re-skin and its revert through its own `crossfade()` (#72). The pixel gate is safe on home for a different reason than on /build: `heroBeat` sets `data-spine="ready"` only after the revert, and the gate waits on that handle. #213 extended the factory block to sample AFTER the interactions that postdate it — the #209/#240 take-over handover and #210's keep-rail copy + export clicks, movement proven first (took over · ?b= in the bar · a download event) — because zero at rest says nothing about a chain that runs afterwards. Operator-run like build-journey (epic #164, tickets #171, #172; factory samples #213)
tooling/vt-stack-audit.mjs   the check to run BEFORE naming anything for a view transition, and the one #172 needs on every page it touches — but NOT YET as an unattended gate: hazard A false-positives on `/index` and `/roundtrip` (2 of the 7 shipped IA pages; hazard B is clean on all seven), because the noise filter discounts elements the engine reports as ANIMATING but not elements with no box at all at sample time. Fix #190 before #172 adopts it, or the red gets ignored. `view-transition-name` is inert in what an element PAINTS, not in how it STACKS: naming it makes it a stacking context AND a containing block for abs/fixed descendants. #171 shipped a real at-rest regression through exactly that — naming the breadboard places promoted them into the same paint step as the absolutely positioned SVG drawing the connection lines, which comes EARLIER in source order, so every line was covered and clipped. The pixel gate cannot catch this class of bug: `update:docker` regenerates the baseline from the same tree, so it re-baselines the regression and the next gated run passes. Two hazards per page: A · does anything MOVE when every name is removed (containing-block shift; elements with running animations are discounted by asking the engine, not by re-sampling), B · does any positioned element overlap a named one from OUTSIDE its subtree with `z-index: auto` (the #171 bug). /build carries a state matrix because its named elements only meet the line overlay in states the gate never captures — connect mode, a derived pack, an emptied board (epic #164, ticket #171)
tooling/proto-journey.mjs    the two proto pages' cross-engine driver, and the one the pixel gate structurally cannot be: that gate never INTERACTS, so it cannot tell a live control from a dead one. Asserts what #176's whole claim rests on — the SAME command from three sources (pointer click · Space on a focused radio · a `source:"agent"` action injected through `bus-toggles.mjs`'s exported `getSlotBus()` seam, never a `window.__` global) producing the SAME resulting DOM class on the SAME tile, with a reset between. Phrased as resulting DOM and never "an action was emitted", which would pass with no consumer at all. Also: `TONES` against the live vocabulary enum, reset against the FETCHED committed proposal, the two refusals (DOM untouched + named in the readout + NOTHING on the console), per-slot isolation, the frame's clamps under drag and keys, `--frame-w` unset at mount, the `@container` reflow as a track COUNT (the pixel string differs per engine), reduced motion, and that work.html's embeds grew no chrome — that last one scrolls the `loading="lazy"` iframes in and waits on the FRAME LIST, because an un-navigated lazy iframe already reports readyState "complete" and the check was vacuously true on firefox + webkit until it did. It caught a real firefox drag bug (see device-frame.mjs). Operator-run like build-journey and vt-verify, not in CI: `node tooling/visual-regression/serve.mjs &` then `node tooling/proto-journey.mjs [engine|all]` (epic #164, ticket #176)
tooling/studio-journey.mjs   the studio canvas's cross-engine driver, and the one the pixel gate structurally cannot be (it never INTERACTS, so a canvas whose controls died stays green). #204's half: the BARE-WHEEL RULE (a plain wheel scrolls and chains to the page — a claim about an event that did NOT happen), arrangement-is-attributes read off the RUNNING page, `fit` against measured layout in both branches, and the far column reachable by Tab. #205's half: the THREE-SOURCE PROOF (pointer · keyboard · an injected `source:"agent"` action through `studio-verbs.mjs`'s `getVerbs()` seam, the third on a FRESH page with no gesture first — that freshness is the whole discriminator, since a mover that applied directly and merely emitted would pass the other two), announcements counted EXACTLY and PER PATH because the paths differ on purpose (pointer 1, keyboard N+2 — a naive once-per-gesture count sends an implementer to delete the per-step keyboard feedback, which is the wrong fix), and THE HIT-TEST IN THREE CONDITIONS — at rest, panned, zoomed — because each is the sole detector of a different missing term in the coordinate chain and each looks correct in the other two. #230's half is the same discipline applied to a component placed AFTER mount: a POINTER drag is the load-bearing case, because a gesture is a PREVIEW and a fix that adopts the node only in the bus consumer records its DESTINATION as its origin — an injected-move case passes against that broken design, and did, 88/88, until a second node moved ONLY by injection gave the consumer's call site its own detector. #229's half completes the SC 2.5.7 click-move-click gesture against the drag to the same cell as its control — the criterion the header is most careful about, and until then the one criterion nothing ran. #209's half is the driver the pixel gate and build-checks group 16 both structurally cannot be: the settled canvas asserted against the COMMITTED board file the page fetches itself (never literals), a BYTE-IDENTICAL settled stage across two loads, every action emitted before the first visitor touch proven `agent.*`/`source:"agent"` with no `ui.move` at all — counted exactly, which needs the artifact fetch DELAYED BY ROUTE, because the first beat fires sooner than a listener can be attached and the count is otherwise silently short by one — pause/step/seek each driven from the keyboard and each announced (parked at a known beat with the seek control rather than slept to, and Resume WAITED for rather than slept past, since a fixed sleep would assert the pacing is uniform, which is the one property paceBeats deliberately does not have), the take-over on a FRESH page mid-replay, its one-shot-ness, the two NON-take-overs (Tab, and the driver's own transport), reduced motion, and destroy() mid-playback. It also caught the race #209 causes in the two passes that came before it: the compile pass and vt-verify both waited on handles that fire at MOUNT and then queried slots the replay had not placed yet. Two constraints, not one, decide where a case may place a node: at this viewport column 9 sits at x≈2025 outside the 1440px window, so an EMPTY cell is not automatically a REACHABLE one (injection is free of that, pointers are not). It caught two real bugs: a queued rAF preview frame discarded on release (a quick drag committed the STALE slot) and Escape unable to reach a body-drag. #213's half is the MEASUREMENT GATE the epic's WRONG-ifs turn on: INP asserted ≤ 200 ms per named interaction per engine — sixteen rows across the settled /factory and a mid-replay page — through a DRIVER-INJECTED PerformanceObserver (helper `tooling/inp-observer.mjs`; nothing ships, the zero-dep pages and pixel baselines stay untouched). The below-16 ms floor is the COMMON case and prints as such — sound only because a forced-slow calibration click proves the delivery pipeline alive per engine first; a bare-click sanity check fails healthy pages, and no calibration lets a dead observer turn every row vacuous-green. One over-budget row re-measures ONCE on a fresh page with both numbers printed, never silently. Plus the 4×-CDP-throttled drag frame check (chromium-only and STATED — worst rAF gap ≤ 50 ms, zero long-animation-frames in the drag window, histogram printed either way), the dock switched to saulera MID-REPLAY with its not-a-take-over assertion (the discriminator is canvas-scoped, and that case is what keeps it that way), and the four zoom verbs activated FROM THE KEYBOARD against exactly the live surfaces the module writes (the aria-live readout for in/out, `.stx-live` for Fit/Reset — asserting the readout IS the announcement assertion). Every bound is printed by the driver itself on every run. Operator-run, not in CI, like build-journey · vt-verify · proto-journey: `node tooling/visual-regression/serve.mjs &` then `node tooling/studio-journey.mjs [engine|all]`. #218's `docsPass` owns the two claims build-checks group 23 states it cannot reach, and says which is which: the LAZY WIRING split in two halves that catch opposite regressions — ZERO requests before Compile (an eager fetch at mount) and a per-url DELTA of zero across four forced re-renders (a `refresh()` that dropped `shouldLoad` and puts three requests behind every undo) — and the CROSS-PAGE FACT, the rendered API and token tables compared string for string against `/components` in a second page. The delta is not a raw count and that is correctness, not weakening: `studio-compile.mjs` fetches `vocabulary.json` on first compile and the Graph panel fetches `system-graph.json`, so "exactly 1 per url" would be RED on a correct implementation; `pack.json` — which nothing else on the page touches — IS asserted at exactly 1. Assertion 5's ORDER is part of the assertion (the token table's live-value column is `getComputedStyle` in two documents, equal only while both wear neutral, so it runs BEFORE the pack swap and says so). Plus: every rendered primitive proven a focusable, described trigger with the count read off the page; the h4/h5 shift; focus opening the docs WITHOUT stealing focus (the sole detector of `activate(i, true)`); the code panels read as COMPUTED display (the `hidden` attribute is inert under an author rule — the whole reason that rule moved into the shared sheet); the inspect bubble still opening after a revert+recompile replaced every node; the toggle off-by-default and persisted; and a 500 becoming a SENTENCE with nothing the page said reaching the console. #219's `framesPass` owns everything group 24 says it cannot reach: both frames rendered at their declared footprints with BOTH handles armed and describing themselves through a resolving IDREF, AC #2 asserted on each frame's own `contentDocument` (no nested dock, inspect toggle, palette or standalone device frame — behind a positive control that the page actually loaded, or the absences prove nothing), the dock's mid-visit swap re-pointing the frame's OWN pack line while still not counting as a take-over, THREE-SOURCE RESIZE PARITY compared on the resulting `data-span-*` with the agent leg on a FRESH page and announcements counted per path (pointer 1 at the drop, keyboard N + 2 including a blocked press), Undo restoring the SPAN and naming it as a size, the MIXED move · resize · move sequence walked back by three Undos in order — the one a per-verb history would fail — ⌘/Ctrl+A leaving both frames unselected (D6's line), a frame stepping clear of the other's WHOLE footprint rather than its top-left cell, and a compile and a redraft both leaving the frames standing at an unchanged HEIGHT. It also carries the one EXPECTED-NOISE filter this driver has (proto-journey's, verbatim) and main-frame scoping on every request log and route fixture, because an embedded proto page fetches the same `vocabulary.json` the compile beat lazily fetches (epic #202 tickets #204, #205, #213, #218, #219)
tooling/instance-journey.mjs   the BUILT-INSTANCE driver (#222), and the one the other drivers structurally cannot be: it drives the OUTPUT of build-instance.mjs — a stamped, self-contained deploy dir — as a visitor would meet it. Builds the Harborlight fixture (tooling/fixtures/harborlight/ — fictional, deliberately NOT the demo company so the residue greps can fail) via the real CLI capturing stdout (the wrangler print + "nothing is deployed" asserted as text — AC #7), serves the dir with an IN-FILE static server (serve.mjs hard-roots the repo and cannot serve an out-of-repo build), then ×3 engines: settled bespoke replay with the stage count read through the page's own INSTANCE_CONFIG → artifact → source.board chain (never a literal), the stamped pack in the head, residue greps on the SERVED bytes comments included (the config <script> block alone carved out — it names the copied run's files by slug, and the fixture deliberately reuses the committed demo run), every internal <a> 2xx, take-over, a COMPILE end to end (which is what puts the lazily-fetched docs chain inside the zero-404 accounting — the operator mutation drill deletes pack.json and watches that row red), and ZERO non-2xx across the whole visit. The fixture pack is verdant's, because saulera's @imports a fonts file this repo does not ship. Operator-run like the other drivers; the pure half (stampShell + auditRefs, with mutations) is build-checks group 25's
tooling/fieldwork-kpis.mjs   ground-truth dispatch KPIs from the Fieldwork fixtures — a post-hoc JUDGE for verifying a composition run's numbers; NEVER fed to an agent prompt (epic #1 ticket #13)
```
The kb (`_factory/kb/` in the jobs folder) is the database — record shapes + parsers → `.claude/references/kb-format.md`.

## Where new code goes
- **Portal API endpoint** → a route branch in `portal/server.mjs` (`readBody` → delegate → `json(res, …)`); logic in a `portal/lib/<concern>.mjs` module, one concern per module.
- **Portal UI feature** → `portal/public/portal.js`: a hash route + render function using the existing `api()` helper; styles in `portal.css`.
- **Machine-layer artifact** → `agent-layer/gen-<output>.mjs` exporting `gen<Name>(ledger)`; register in `build.mjs` (import + call + `✓` log line), keep the standalone-run guard. Shared parsing belongs in `lib.mjs`.
- **Component** → token-only CSS in `system/components.css`; a new semantic token gets added to `system/tokens.source.json` (contract group) first, then regenerate: `node agent-layer/gen-token-css.mjs`.
- **New component spec** → `system/specs/<component>.md` (+ `.contract.json` if data-bound) per `.claude/references/kb-format.md`, then regenerate the pack: `node agent-layer/gen-handoff.mjs`. The chain is not finished at the spec: a component also needs its **`components.css` block** (header `/* ---------- <class> (system/specs/<name>.md) ---------- */`, token-only) **and its `agentic-renderer.mjs` template**, because `build-checks` group 3 asserts that EVERY generated vocabulary entry has a render path — widened from the emitted set by #211, which closed `demo-notice`'s gap and deleted the written-down exception. A spec with a vocabulary entry and no block and no template is *documented but not composable*, and it is now a red build rather than a comment. The optional `example` head key (the live playground's starting props) is validated SEMANTICALLY at generation time — it must actually render, or CI `verify` goes red naming the spec.
- **New /build pattern** → a rule in `system/pattern-rules.mjs` (the rule NAMES the pattern from the board and the slots are COUNTED from it, never invented) + its entry in `PATTERNS`. Spec-first, same as any component: it may only compose components that already exist in `system/specs/` and validate against the generated `handoff/verdant/vocabulary.json` — a pattern with no components renders the honest out-of-library refusal card in `system/pattern-render.mjs`, which is the correct outcome, not a gap to fill with markup. All five current patterns are in the library (#139), so that card is unexercised — it is the sixth pattern's contract, and `build-checks` group 1 keeps a deliberately vacuous `inLibrary: false ⇒ needs` clause guarding it. Add a `BOARD_FOR` fixture in `tooling/build-checks.mjs` too: every group iterates `PATTERNS`, so a new entry with no board fails loudly rather than being silently skipped. Then `node tooling/build-checks.mjs` (groups 1–3 and 6 cover it) and `node tooling/build-journey.mjs all`.
- **WC wrapper** → `system/wc/<tag>.mjs`, spec-first (a wrapper exists only for a `system/specs/` component; shadow CSS uses only spec-head tokens, no literals, no var() fallbacks), copied into the pack by `gen-handoff`.
- **Brand/company skin** → clone `system/tokens.neutral.css` → `tokens.<company>.css` and `client.neutral.config.js` → `client.<company>.config.js`; never fork components.
- **Pack imported from Figma** → `node tooling/figma/figma-pull.mjs --slug <slug> --accent <hue> [--page Color]` → `system/tokens.<slug>.css` (`--offline` re-runs off the cache for free; `--from <export.json>` reads a plugin export instead of the API — no token, no quota, no Enterprise gate). It targets a PACK, never the contract — a Figma file holds brand values, and the contract layers stay generated from `tokens.source.json`. Same honesty rule as traces: the pack header states whose design work it is, which ramps were mapped, and every contrast negotiation and remaining WCAG failure.
- **View-time behaviour on shipped pages** → a hand-written ES module beside `system/site.js`.
- **New live-manipulable control on a shipped page** → add its `system/param-manifest.json` entry in the same PR + regenerate `node agent-layer/gen-param-count.mjs` (CI `verify` drift-checks it; an omitted control is a review-catchable gap).
- **kb record type or field** → `.claude/references/kb-format.md` (both parsers must stay in sync).
- **New scenario** → clone a `scenarios/<slug>/` package per `scenarios/README.md` + one `scenarios/index.json` entry + its imports in `worker/fixtures.mjs`; the Worker's routes (`worker/api.mjs`) never change.
- **New trace** → record a REAL run: `node portal/record-trace.mjs` → curate `node tooling/curate-trace.mjs <raw> <out>` → validate `node tooling/validate-trace.mjs`. Hand-writing or hand-editing trace content is forbidden (honesty contract, hard) — a bad run is fixed by a tighter agent prompt + re-run, never an edit.
- **New replay run** → a REAL run, the trace rule extended: write the brief by hand (`replay/briefs/<slug>.md` — the problem, never the answer, and no board), prove the mechanism with `node portal/record-build.mjs --slug <slug> --dry`, record it with `node portal/record-build.mjs --slug <slug>` (`--force` to re-run a slug — it clears the stale board AND the stale artifact first), then project it with `node agent-layer/gen-replay.mjs`. The slug names four files and `traces/` is a FLAT namespace, so it must be globally unique. **Never hand-write or hand-edit a board, a trace or an op** — a weak run is fixed by tightening `PIV_BUILD_SYSTEM`/`buildTask` in `portal/record-build.mjs` and re-running. A new op verb is a `system/board-ops.mjs` edit (the `OPS` list and its `PARAMS` entry and the switch, together) plus a build-checks group 11 case, never a special case in the generator.
- **New composition proposal** → a REAL run. **UI-first path (preferred):** the portal's "Compose a view" drawer — answer /build's ten method questions, and `portal/lib/builder.mjs`'s three committed rules draft the question from two of them (`shape`, `action`); the drafted question is EDITABLE before the run, the slot's bounds and the ethics verdict show beside it, and the PIV phases stream live (`/api/build/{config,draft,run}`). Leave the `--dry` box checked for the first run: a dry run is a full agent run over the real fixtures that writes nothing, so `in-process validateComposition ✓` is what proves the question is answerable before a real one is spent. **The equivalent CLI:** `node portal/record-composition.mjs <scenario> "<question>" <slot> [--slug <slug>]` (`--dry` first to prove auth). The scenario must carry a `scenarios/<scenario>/compose.json` (slots come from it — Fieldwork's are `summary-strip|insight-panel`). It writes `proto/compositions/<scenario>/<slug>.json` + a validating PIV trace pair and upserts the scenario-scoped `proto/compositions/<scenario>/index.json` (Fieldwork's legacy root artifacts stay at `proto/compositions/` and are only read); verify the numbers against the fixture (Fieldwork has `node tooling/fieldwork-kpis.mjs`). Same honesty rule as traces — never hand-write a composition or hand-feed an example; the `compose.json` computeRules carries DEFINITIONS ONLY (never names which tiles answer a question); a weak/invalid run is fixed by tightening the runner's committed prompt or the scenario's computeRules + re-run.
- **Platform capability (epic work)** → check `docs/epics/ai-first-ux-factory.architecture.md` first — most "new" pieces are already-decided Missing pieces with format and placement pinned.

## Ground rules (conventions)
- **Shipped pages are vanilla — hard constraint.** No framework, no bundler, no build step, no runtime deps, no live LLM calls at view time (agents run at build time; readers replay). Factory tooling is unrestricted but stays zero-dep Node ESM where possible — the portal's sole dependency is `@anthropic-ai/claude-agent-sdk`.
- **Token discipline:** components reference semantic tokens only — a brand value or literal in `components.css` is a bug. Mechanic + how to add a token → `.claude/references/token-system.md`.
- **Types:** plain JavaScript — no TypeScript, no schema library. Validate by hand at the boundary and throw (`portal/lib/intake.mjs`).
- **Errors:** throw plain `Error`s whose message names the offending path (`agent-layer/lib.mjs`); one catch-all at the server boundary returns `{ error }` JSON (`portal/server.mjs`). No error taxonomy, no wrapping.
- **Feature/entry-point files open with a header citing their governing doc** (`strategy §13`, `RUNBOOK P11`, `_factory/agent-layer.md §4`); helper modules get a plain what/why header. New files follow suit.
- **Secrets:** only in `portal/.env`, hand-parsed by `portal/lib/env.mjs` (no dotenv) — never committed, never client-side.
- **Honesty contract (hard, from the PRD):** fictional scenarios visibly labeled; traces labeled "real run, curated"; capability indicators state exactly what runs vs. what's plan-gated. Never hand-write anything presented as agent output — re-run with tighter prompts instead.
- **Git:** work on `main`; one atomic commit per phase/ticket, message = what + doc reference (`portal V1: … (strategy §13, P11)`). **A PR body MUST carry a `Closes #N` trailer** for the ticket it finishes — a title that merely mentions `(#N)` does not close anything, so merged tickets sit open and look unplanned (#78 shipped in PR #93 and stayed open for a day, costing a wasted planning pass). **A ticket's plan, report and review belong in the same PR** (`.claude/plans/`, `.claude/reports/`, `.claude/code-reviews/pr-<N>-review.md`) — four of PRs #97–#100's artifacts were written but left uncommitted in their worktrees, one `git worktree remove` away from being lost. **Deploy = commit the artifacts:** generators run at authoring time, outputs are committed, Pages serves the repo as-is — the repo itself is inspectable proof for technical readers, so never hide generated outputs behind a build step or `.gitignore`.
- **Testing:** no suite, no linter, no type-check — don't hunt for or invent one. "Done" = run the surface you touched: portal boots and `/api/health` answers; a generator prints its `✓` line; a page renders under the neutral pack.

## Working principles
- Platform work is governed by `docs/epics/ai-first-ux-factory.prd.md` + `.architecture.md` — if a change would contradict a decision recorded there, stop and flag it; don't silently drift.

## Commands
- portal: `cd portal && npm install && npm start` → http://localhost:4747
- static shell preview: `npx serve .` (repo root)
- agent layer (from the jobs folder): `node ../ux-factory/agent-layer/build.mjs _factory/kb/decisions/<company>.md`
- per-company instance (from the jobs folder; **--out MUST be outside the repo**): `node ../ux-factory/agent-layer/build-instance.mjs <brief.md> --out <dir> --pack <tokens.<slug>.css> --trace <derivation.jsonl> --replay <slug> [--handoff <url>]` — builds the deploy dir + prints the deploy command (`--replay` names a recorded build run — record it with `portal/record-build.mjs` + `gen-replay.mjs` into the WORKING TREE for a real company and never commit it; the five files are copied, never recorded here); then run `npx wrangler pages project create inst-<slug>-<rand> --production-branch main` and `npx wrangler pages deploy <dir> --project-name inst-<slug>-<rand> --branch main` (unlisted + noindex; deploy is the operator's explicit step)
- deploy: `npx wrangler pages deploy . --project-name factory-ux --branch main`

## On-demand context
- API route work → `.claude/references/backend-api-best-practices.md` · UI work → `.claude/references/frontend-component-best-practices.md`
