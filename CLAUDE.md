# CLAUDE.md — ux-factory

## What this is
The code repo for an AI-first UX-engineering portfolio platform: a token-contract design system that re-skins an entire site from one line of CSS, plus the build-time "factory" around it — Node ESM generators and a local portal workbench — being extended into a pipeline that runs visibly in front of the reader (intake → generated design system → data-connected prototype → handoff pack) so hiring managers can verify senior UXE skill instead of trusting claims. This repo holds code only; per-application content, the pipeline skill, and the knowledge base live in the sibling jobs folder (`../Linards jobs folder/_factory/`). Intent and decisions: `docs/epics/`.

## Architecture map
```
system/                     the shipped design system — brand-agnostic core + packs
  tokens.source.json        DTCG source of truth — gen-token-css.mjs generates the two CSS layers below from it
  tokens.contract.css       layer 1 (GENERATED): every semantic token components may use, with neutral fallbacks; no brand ever
  tokens.neutral.css        layer 2 (GENERATED): the neutral pack; a company build clones it to tokens.<company>.css
  components.css            layer 3: token-only components — no literals, render identically under any pack. Site CHROME that must reach the proto pages lives here too, not in portfolio.css, because the protos load this sheet and not that one: the ⌘K palette (#168), the appearance dock + ruler and the inspect bubble (both moved in by #175). portfolio.css's global reduced-motion kill-switch does not reach here, so a chrome block carries its own
  portfolio.css / proto.css surface styles for portfolio pages / prototypes
  site.js                   injects chrome (header/footer/nav) from window.CLIENT_CONFIG
  pack-boot.js              pre-paint pack restore on all ten shipped pages (classic script; the two protos joined at #175): re-points the one tokens.<pack>.css head line from localStorage, hard-allowlisted, guaranteed no-op by default (VR relies on that), and its tag must stay the LAST element in every head that carries it (portfolio-ux-uplift §Phase 5)
  dock.mjs                  appearance dock (hand-written canon): pack switcher across the packs in its own hard-allowlisted `PACKS` list — neutral · saulera · verdant · plusui as of #129, and adding a pack means editing that list AND `PACK_RE` — plus copy-tokens + left scroll ruler; hash-routed #appearance disclosure, persisted choice restored by pack-boot.js. On all ten shipped pages since #175, but the two protos import it only when `window.self === window.top`, so work.html's iframe embeds get the reader's pack without a nested rail (portfolio-ux-uplift §Phase 5)
  analytics.mjs             CF Web Analytics beacon (public token + production host, both filled at launch; node-safe behind a `typeof document` guard, because tooling/build-checks.mjs imports two modules that import this one) + the virtual-route event helpers — CF WA has no custom events, so an event is a pageview at a SYNTHETIC PATH: /factory/{driven,built,shared,arrived} on home, /build/{pattern,shared} on the builder. Each fires ONCE from its own success path — never from a slot or a settled-state flag that also runs when the thing did not happen — and each path is a static literal, because the path is the entire payload. The `/build` pair share one restore, `flipTo`, which handles two collisions with the 50 ms window that the four `/factory` trackers do not. It puts the HASH back live — a disclosure opened inside the window (the dock) would otherwise be left open with no `#appearance` for Escape to match, measured on all three engines (#149). And it records the real URL ONCE across OVERLAPPING windows — a second flip opening inside a first one's 50 ms would otherwise snapshot the VIRTUAL path and restore the reader to it permanently, which 404s on reload; reproduced 4/4 on chromium, where the Copy click's own scroll is what starts the vocabulary fetch (PR #162). It also exports `onVirtualRoute()`, which is a CONTRACT and not a helper: `build-keep.mjs` builds the share link out of `location` and waits on it, because `shareUrl` only overwrites `?b=`, so a link built mid-flip keeps `/build/pattern` and 404s. The four `/factory` trackers still take a per-flip snapshot and read nothing live: both collisions are reachable there in principle, neither has been observed, and #149 deliberately did not touch them (tickets #6, #75, #77, #149; PR #162 review)
  derive.mjs (+ oklch.mjs, wcag.mjs, derive.rules.mjs)   view-time derivation engine: intake answers → token values + WCAG checks + ethics verdict; driven raw by /derive.html, spike runner in tooling/ (epic #1 ticket #3)
  agentic-renderer.mjs + action-bus.mjs   declarative agentic bridge (hand-written canon): vocabulary-validated {name,props,children} → real components (refuses everything else) + one bidirectional action contract (agent/click/keyboard, voice-ready); driven raw by /agentic.html (epic #1 ticket #11)
  trace-player.mjs          view-time trace replay (hand-written canon): parseTrace + renderTracePlayer render a committed trace as stepped cards in the four PIV acts; driven raw by /trace.html, designed surface = Factory page (epic #1 ticket #5)
  agentic-study.mjs         designed ask → propose → adjust surface over the bridge (hand-written canon): renderStudy replays committed composition proposals, adjusts a deep-cloned working copy live via the renderer + bus, and shows the refusal as a primary affordance; built on the `metric-tile` library primitive (class `ds-`, cross-scenario, NOT vd-/fw-); driven by /agentic-ui-study — the designed successor to /agentic.html — and mounted a second time by instance.mjs for the private instance's bespoke prototype slot (`subject` names what the run ran over) (epic #1 ticket #13; reuse epic #86 ticket #89)
  handoff-viewer.mjs        view-time handoff-pack viewer (hand-written canon): prepareHandoff + renderHandoffViewer show each component's spec head · engineer docs · agent vocabulary side by side from one generated source (private markdown-subset prose renderer); driven raw by /handoff.html, designed surface = Factory page (epic #1 ticket #14)
  annotated-source.mjs + glossary.mjs   legibility surfaces (hand-written canon): render build-time-extracted real-code snippets (system/annotated-source.json, generated by agent-layer/gen-annotated-source.mjs from its spec — drift-checked) + WCAG 1.4.13 term-definition bubbles on approach.html and, since #173, factory.html — initGlossary runs inside the graph mount module so an unknown key aborts before the ready handle and the VR gate fails loud (docs/epics/annotated-source-glossary.architecture.md)
  derive-probe.mjs          view-time probe beside approach's derive snippet (hand-written canon): runs the real derive() on a reader-chosen brand colour and reports the negotiated accent, the measured contrast pair + the engine's notes verbatim — the quoted code and the running code are one file
  board-ops.mjs             the studio's BUILD-OP vocabulary + a pure applier over breadboard.mjs's board model (hand-written canon, DOM-free): the eight ops (`place.add` · `place.rename` · `place.remove` · `affordance.add` · `affordance.rename` · `affordance.remove` · `connect` · `disconnect`), `applyOp`/`applyOps` (pure — a NEW board every time), `assertBoard`, and `parseOpCommand`. In `system/` because three layers need it: the fenced recorder's CLI, the generator's reproduce check, and #209's view-time driver. Two invariants: the caps are IMPORTED from breadboard.mjs, never re-literalled — and an op NEVER carries an id for the thing it creates (`place.add` takes a label), so ids are assigned here and gen-replay's reproduce check genuinely re-derives them instead of copying them out of the trace. `parseOpCommand` is deliberately shared by record-build.mjs's FENCE and gen-replay's PROJECTION: one grammar, so a command the projector could not read is denied mid-run rather than discovered after the run is paid for (epic #202 ticket #203)
  bus-toggles.mjs           Fieldwork's agentic-slot state commands (hand-written canon): the ONLY code in the repo that exercises the `agent.*` half of action-bus.mjs — every other emit is `ui.intent`, so the header's "adding a modality is a new `source`, not a new bus" was documented and nowhere demonstrated. mountSlotToggles emits `agent.set-tone` and a bus CONSUMER applies it to a deep-cloned working copy re-rendered through renderComposition (validate-before-DOM intact); action-bus.mjs is NOT edited and must not be — the point is that a consumer was all this needed. Refusals go to the aria-live readout, never a throw: action-bus.mjs:70-77 catches handler throws into console.error, which would hide the refusal from the reader AND trip proto-journey's no-page-errors contract. Each row filters on `target.id`'s slot part, so an action for the other slot is ignored rather than refused — the refusals it owns are an out-of-range index and a tone outside `TONES` on a MATCHING slot. Its `TONES` is a second copy of an unexported const in agentic-study.mjs, so proto-journey asserts it against the live vocabulary's enum (epic #164 ticket #176)
  device-frame.mjs          Verdant's resizable device frame (hand-written canon): pointer drag + WAI-ARIA window-splitter keys, clamped against MEASURED available width (at a 320px viewport the CSS default `94vw` already sits below FRAME_MIN, so a bare-constant clamp would widen the frame off-screen on the first keypress). REST GEOMETRY STAYS CSS — proto.css declares `width: var(--frame-w, min(392px, 94vw))` and this module writes `--frame-w` only once the reader acts, so nothing races the mount and the pixel gate captures what it always did (pack-boot.js's guaranteed-no-op discipline). A `pointermove` carrying no primary button ENDS the drag: firefox keeps delivering moves at clientX 0 once a captured pointer leaves the window, which snapped the frame to its minimum until proto-journey caught it. Sets [data-device-frame="ready"] in a `finally` on every path, including the no-phone early return (epic #164 ticket #176)
  loc-summary.json          GENERATED (agent-layer/gen-loc-summary.mjs, drift-checked): measured file/line counts of the shipped groups, lines rounded to the nearest 100; approach.html renders its numbers — a size claim is never hand-written
  param-manifest.json       hand-maintained manifest of every live-manipulable control on the 10 shipped pages + chrome (one entry per control; counting rules in its $description) — the one hand-written input to gen-param-count (epic #164 ticket #167)
  param-count.json          GENERATED (agent-layer/gen-param-count.mjs, drift-checked): per-page + site-wide control totals counted from param-manifest.json, exact not rounded; approach.html renders the total — a capacity claim is never hand-written
  system-graph.mjs          view-time "shape of the system" exhibit (hand-written canon): prepareGraph + renderSystemGraph draw the generated token↔consumer graph as factory.html's #shape section — edges on hover/focus only, pack values verbatim (portfolio-ux-uplift §Phase 5). #173 added the bounded pan/zoom window: drag-to-scroll on a native scroller (Tab order survives), ⌘/Ctrl-wheel + trackpad pinch, explicit in/out/reset buttons as the keyboard path, one SVG width write against the unchanged viewBox — at rest scale 1 / scroll 0,0, so the hidden-at-capture panel never races the pixel gate
  system-graph.json         GENERATED (agent-layer/gen-system-graph.mjs, drift-checked): contract tokens × components.css consumer blocks × the three packs' raw bindings — the graph is measured, never hand-drawn
  derivation-roundtrip.mjs  view-time Verdant derivation round-trip exhibit (hand-written canon): prepareDiff + renderRoundTrip render #40's committed fidelity diff (proposed-vs-truth ΔE · human gate · verdict · 12 WCAG pairs) beside the mounted pack-seed-verdant trace — static honesty framing + JS-rendered numbers, fetches committed files only; designed surface = Factory page #round-trip exhibit (epic #38 ticket #42)
  instance.mjs              view-time private-instance shell config (hand-written canon): reads window.INSTANCE_CONFIG → fetches a scenario package → honesty notices (fictional + speculative + sources) · curated intake with reasoning · the SHARED wizard via factory-intake.mjs's initIntake(config) seam (configured, never forked) · embedded pack-seed trace · the BESPOKE PROTOTYPE rendered in place (INSTANCE_CONFIG.composition → the SHARED renderStudy surface, configured not forked; falls back to a link, then an honest placeholder) · the handoff link slot; driven by instance.html — the Factory-station variant a real application deploys unlisted (epic #38 ticket #43; prototype slot epic #86 ticket #89)
  ── the /build chain (epic #134): the visitor's OWN tokens + the visitor's OWN product. Five mounted
     entry points + three pure modules, all hand-written canon and all committed-rules-in-the-browser
     — nothing uploads, no model is called ──
  build-import.mjs          Act 0: reads a dropped .json token export IN THE BROWSER (DTCG · Tokens Studio · any nested name→value), maps it onto the contract by ROLE, and dresses BOTH stages; 32 MB cap; the report states whose design work it is before it says anything else (ticket #135)
  build-questions.mjs       the shared answer store + the BUILD_CHANGE contract + both acts' stepped wizards (Hooked ×7, Shape Up ×3) + the verdict panel (Manipulation Matrix quadrant · frequency filter), one mount per act over one answer set (ticket #136)
  breadboard.mjs            Act 3: Shape Up's breadboard, drafted from the ten answers and then editable — rename · add · remove · re-point, every verb announced in one live region and every verb placing focus; LABEL_MAX is the cap both the inputs and the codec share (ticket #136)
  pattern-rules.mjs         the three committed rules that name a pattern from a board (1 the `shape` answer names it, 2 the hub override reads the BOARD and outranks the answer, 3 an empty board names none) + the slots COUNTED from the board, never invented, for ALL FIVE patterns since #139. `SLOT_MAX` truncates exactly one of them (feed reads the whole board, so 36 candidates can become 6) and `affordanceCount` is how the surface states that (tickets #137, #139)
  pattern-render.mjs        Act 4: renders the named pattern through the site's own vocabulary-validated agentic-renderer — dashboard→metric-tile, queue/feed/settings→list-row, onboarding→sequence-step; feed and settings share a primitive because their derived slot IS list-row's contract, and what differs is arrangement (build.html's .bx-pat-slots.is-*). The out-of-library refusal card is RETAINED and currently unexercised: it is the contract a SIXTH pattern gets, not a fake. Sets [data-pattern-stage="ready"] only after an IntersectionObserver-gated vocabulary fetch — a VR waitVisible handle, never waitReady (tickets #137, #139)
  build-card.mjs            the build's SVG artifacts (build card + breadboard), escaped once, hostile labels and tokens neutralised at the template (ticket #137)
  build-share.mjs           the whole build in the URL: deflate-or-raw codec + the tamper battery's counterpart — every payload validated whole, caps enforced, bidi controls and magnitude bombs rejected (tickets #137, #138)
  build-keep.mjs            Act 5: the keep rail — card, four downloads, the share link, and the ?b= restore; hides all three artifact tiers when the board is bare (ticket #137)
  pack-import.mjs           the token-import ENGINE, ONE never a fork: a design's token export → a contract pack, role-mapped and contrast-negotiated. The Figma CLI, the portal drawer, home's drop zone and /build's Act 0 all map through THIS file, or a browser would map a design differently from the CLI that produced the committed packs (ticket #130)
  brand-import.mjs          HOME's public drop-to-re-skin surface (beat 02): the drop zone, the mapping report with its WCAG receipts, "wear it" across the visit, and a tokens.<slug>.css download byte-identical to the CLI's (ticket #130)
  pack-imported.mjs         the imported-pack RECORD (sessionStorage, sibling of pack-derived.mjs) + `vetTokens` — the allowlist every visitor-supplied value passes through before it reaches the DOM, on home and on /build alike; tooling/build-checks.mjs's vetting group guards it (ticket #130)
  tokens.css / tokens.saulera.css / client.config.js   real packs kept as reference; NOT loaded by the shell
  specs/                    ComponentSpec .md + DataContract .json — the handoff source of truth (format: .claude/references/kb-format.md)
  wc/                       custom-element wrappers (vd-*) + demo.html — hand-written canon, spec-first, shadow CSS reads only spec-head tokens (epic #1 ticket #12)
  figma-import.md           DTCG→Figma import-path doc; ships in the pack via gen-handoff
index.html + approach/factory/work/contact.html + 404.html   the shipped five-page IA on the neutral shell — Home is the 90-second recruiter gate; factory.html reserves the deep-linkable route as an honest stub (epic #1 ticket #6)
build.html                  the pattern builder — the sixth public surface, and the only one about the READER's product: their token export (or one colour) → the ten method questions → an editable breadboard → a real UI pattern under their brand → a link that rebuilds the whole build. OFF-nav and `noindex`, linked from the home close card, work.html's "Run it" grid and the footer site index (#148 — the footer claims to be the full index, so it had to be; one chrome item churns all 16 chrome-bearing baselines, the two proto pages carrying none). Six acts over five module entry points (build-import · build-questions · breadboard · pattern-render · build-keep), which pull in the three pure ones below; no upload and no model call (epic #134, tickets #135–#138, #148)
instance.html               the deep-link-only private-instance shell (off-nav, not in the five-page IA): a Factory-station variant a real application deploys unlisted; demo-configured to scenarios/northwind. Carries build-instance.mjs's stamping seams — INSTANCE_CONFIG markers + data-when="demo"/"real" prose regions — that #44 rewrites demo→real per company (epic #38 tickets #43, #44)
_headers                    security headers + noindex (revisit at launch — epic open question)
agent-layer/                build-time Node ESM generators: machine-readable projection of one site
  build.mjs                 orchestrator — run FROM THE JOBS FOLDER against a decisions ledger
  build-instance.mjs        SIBLING orchestrator (NOT a gen-*, not in build.mjs): compiles one company brief (gen-company-package) + a derived pack + a trace + optional pre-recorded composed views (`--compositions <dir>` — COPIED in with handoff/verdant/vocabulary.json and gated by validateComposition, never generated here; same copy-not-run discipline as `--trace`) + the #43 instance.html shell into a self-contained deploy dir OUTSIDE the repo, stamps the shell demo→real, emits a standalone noindex _headers, and PRINTS the wrangler direct-upload command — deploy stays a human step; nothing company-real is committed (epic #38 ticket #44, folds spike 2; bespoke-prototype step epic #86 ticket #89)
  lib.mjs                   ledger parser + shared helpers
  gen-replay.mjs            GENERATED + drift-checked: projects a committed build run into `replay/<slug>.json` — the ops a driver plays, `{op, atMs, phase, params, fromStep}`, with `atMs` the run's REAL pacing and every `source` value copied from the curated meta. Its `projectTrace` is PURE so build-checks group 11 can drive it. Three things a future editor must know: it projects from the CURATED trace, which works ONLY because `tooling/curate-trace.mjs:17` keeps `command` in `KEEP_WHOLE` (every other input string clips at 700 chars); the REPRODUCE CHECK re-applies the projected ops and demands they rebuild the committed board exactly, which is what makes the projection a claim about the run rather than about itself; and discovery runs in BOTH directions, so an orphaned `replay/<slug>.json` with no board behind it is an error rather than an invisible stale file (epic #202 ticket #203)
  gen-*.mjs + inject-jsonld.mjs   one file per emitted artifact (canon token CSS, decisions.json, DTCG tokens.json, llms.txt, _headers, JSON-LD); each runnable standalone
portal/                     local-first workbench (127.0.0.1 only, never deployed)
  server.mjs                zero-dep node:http server — thin route dispatch, logic in lib/
  lib/                      one concern per module: env.mjs (paths + .env), kb.mjs (card projections), intake.mjs, chat.mjs (Agent SDK behind SSE), trace-recorder.mjs (Agent SDK hooks → Trace JSONL), origin.mjs (the CSRF guard `server.mjs` applies before ANY routing: binding 127.0.0.1 stops a remote client but not a page in the operator's own browser, and `readBody` JSON.parses regardless of content-type, so a no-preflight cross-origin POST would otherwise reach a token-spending run. Accepts BOTH `http://localhost:<PORT>` and `http://127.0.0.1:<PORT>` — reject one and the drawer, intake form and chat pane break for whichever the operator typed — and an ABSENT Origin, which is every curl and every same-origin GET. build-checks group 9 gates the predicate; the WIRING is only ever proven against a running portal, since `server.mjs` reaches the SDK) (#157, deferred from PR #156's review), builder.mjs (the OPERATOR PATH: /build's ten answers → the question a real composition run answers — three committed rules (`shape` names it, `action` names who for, the other eight enter no prompt), the `fictional:false` privacy refusal, the run lock and the SSE whitelist; imports the question config from system/build-questions.mjs, never forks it, and its whole graph is SDK-free so tooling/build-checks.mjs can gate it in CI) (epic #134 ticket #140)
  public/                   vanilla SPA — hash routing, template strings, no framework
  record-trace.mjs          build-time trace recorder run (CLI, not deployed): wraps the Agent SDK in hooks, emits traces/<slug>.raw.jsonl — a REAL agent run (epic #1 ticket #5)
  record-composition.mjs    build-time composition runner (CLI, not deployed): a REAL Agent SDK run composes a {name,props,children} dashboard view for one question about one SCENARIO → proto/compositions/<scenario>/<slug>.json + a scenario-scoped index.json + a PIV trace pair; prompt built ONLY from vocabulary + the scenario's declared fixtures + question + slot bounds (no example); the scenario is parameterized via scenarios/<slug>/compose.json (subject/today/fixtures/copy/slots/computeRules[DEFINITIONS-ONLY]/questions — the Read fence is rebuilt from it), so the runner has no baked-in scenario; reuses recordRun + validateComposition + curateTrace; keep-gate is validateTrace (epic #1 ticket #13, folds spike 6; parameterized epic #86 ticket #88)
  record-build.mjs          build-time INCREMENTAL build recorder (CLI, not deployed) — record-composition.mjs's sibling, and diffable against it: a REAL Agent SDK run reads one human-authored brief and assembles a Shape Up breadboard ONE OP PER TOOL CALL, so one call is one op is one trace step (a composition run's implement act is a single Write, which projects to a cut rather than a build). The fence is tighter in the one direction that matters: Write/Edit DENIED OUTRIGHT, Read = the brief only, Bash = only what `system/board-ops.mjs:parseOpCommand` accepts, running THE build tool (resolved as a path IDENTITY — a filename suffix is not one) against THIS run's board. **A build run's `artifacts` count is therefore 0 BY DESIGN** — the Trace format's Write→artifact pairing never fires, and nothing in the trace links it to `replay/<slug>.board.json`; that link is enforced by gen-replay's reproduce check instead, deliberately, because un-denying Write to recover the format-level pairing would let the agent write anything to that path on the run whose whole subject is fence discipline. Same lazy-SDK import, same `--dry`/real split, same drop-on-failure ship gate (epic #202 ticket #203)
scenarios/                  scenario packages — the demo subjects + fixtures (format: scenarios/README.md; validate: node scenarios/validate.mjs; check page: /scenarios/check.html)
worker/                     fixture-backed mock API — public read-only GET, one Cloudflare Worker (cd worker && npx wrangler dev); site degrades to static fixtures via system/scenario-data.mjs
proto/                      data-connected prototype pages — Verdant phone screen + Fieldwork hybrid canvas (vd-/fw- components implemented to system/specs/); Fieldwork's two agentic slots render #13's canonical proposals. Since #175 both wear the site's pack skin (pack-boot + a top-window-only dock) and carry inspect mounts — and because their components render from an AWAITED fetch, each re-inits inspect.mjs after its innerHTML lands, or a reader with inspect persisted on gets a screen wired to nothing. Since #176 both are also MANIPULABLE rather than view-only: Verdant's bezel is a draggable/keyboard-resizable frame whose screen reflows through `@container` queries (the queries live in proto.css, never components.css — a rule there would move system-graph.json → inspect-data.json → factory's baseline), and each of Fieldwork's FILLED slots carries a control row commanding its tiles over the action bus. Both mounts are top-window only, so work.html's iframe embeds stay bare; both set a VR readiness handle in a `finally` ([data-device-frame], [data-bus-toggles]) because both paint at-rest chrome that arrives after load
proto/compositions/         COMMITTED composition proposals (one {name,props,children} JSON per question) + index.json manifest — generated by real record-composition.mjs runs; consumed by the study page + the Fieldwork slots (epic #1 ticket #13)
traces/                     committed real agent-run traces (format: traces/README.md): raw + curated JSONL pairs from portal/record-trace.mjs (+ record-composition.mjs for #13's composition runs, + record-build.mjs for #203's build runs); validate: node tooling/validate-trace.mjs; replayed by system/trace-player.mjs
replay/                     the studio's replay artifacts (format: replay/README.md) — and the rule the whole tree exists to state: **a replay artifact is a PROJECTION of a committed real run, never a recording.** The recording is the trace pair in `traces/`, committed beside it under the standard labels and named in the artifact's `source`; when the two disagree the trace is the truth and the artifact is regenerated, never the other way round. Three files per run: `briefs/<slug>.md` (human-authored INPUT — a person writing the problem statement is not the honesty contract's subject, and the brief states the problem, never the answer), `<slug>.board.json` (the run's real OUTPUT, what the ops built) and `<slug>.json` (GENERATED by gen-replay.mjs, drift-checked). Never hand-write or hand-edit any of the three (epic #202 ticket #203)
docs/epics/                 PRD + architecture decisions governing the platform build
docs/figma-runbook.md       operator steps for the Figma boundary: import a design as a pack (once per design, one command) vs the parity round-trip (once, ever) + the request-budget rules
handoff/                    GENERATED handoff pack (verdant/) — committed, never edited by hand; regenerate: node agent-layer/gen-handoff.mjs (pack + tokens + wc wrappers) · node agent-layer/gen-vocabulary.mjs (vocabulary.json — the agent-ready layer the renderer validates against) · node agent-layer/gen-pack-bundle.mjs (pack.bundle.json — every pack file inlined as one downloadable bundle, the /handoff.html download); figma-parity.json is the one exception: written only by the parity script's real run
tooling/figma/              the Figma boundary — secret-gated (portal/.env), standalone-only, never registered in build.mjs
  figma-read.mjs            the SHARED read both scripts use: auth, the Enterprise variables gate, the paged fallback, and the response cache that conserves a Starter file's ~6 GET-file/month (#110). `--from <export.json>` skips the API entirely — a plugin export has no rate limit AND sees the variables REST gates behind Enterprise
  figma-parity.mjs          READ-BACK: diffs a Figma file against the token contract → handoff/verdant/figma-parity.json
  figma-pull.mjs            IMPORT: a Figma file's colour ramps → system/tokens.<slug>.css, mapped by ROLE (ramps share no names with the contract), contrast-negotiated within the file's own ramps against RULESET.wcagPairs — the same list derive() is held to
tooling/mcp/                local MCP helper scripts
tooling/style-dictionary/   a dependency-carrying tool; emits the pack's multi-target tokens (css/ios/android)
tooling/visual-regression/  isolated Playwright (the other dependency-carrying tool); CI visual-regression gate — screenshots the 10 shipped pages (6 IA + /roundtrip + /build + 2 proto) under neutral + saulera packs, pixel-diffs vs committed Linux baselines. Capture = measure → resize → wait for any activateOn:'visible' beat → RE-measure until the height stops moving; skip that last step and the shot silently truncates whatever the beat added (#9 gate 3/3; /build + the re-measure fix #138)
tooling/wc-sandbox/         React 19 harness for the wc wrappers (esm.sh import map, no install)
tooling/curate-trace.mjs · validate-trace.mjs   deterministic trace curation (selection + truncation only, rules recorded in meta) + the Trace format's drift guard (candidate CI gate #9)
tooling/board-op.mjs        the fenced build agent's ONLY build tool (epic #202 ticket #203): `node tooling/board-op.mjs <board.json> '<op json>'` applies exactly ONE op and prints the whole resulting board back, ids included — so one Bash call is one op is one trace step, and the agent learns the machine-assigned ids from real output instead of inventing them. `--validate` is its assertBoard form. It runs the SAME `parseOpCommand` the fence denies with, so there is no third opinion about what an op call looks like. It writes wherever it is pointed on purpose: the RECORDER'S FENCE, not this file, constrains the target — a second path allowlist here would be a second copy of the fence, and it would drift from it
tooling/build-checks.mjs · build-journey.mjs   /build's two gates. `build-checks` is PURE (imports the shipped modules, opens no browser) and RUNS IN CI (verify job): 11 groups — pattern rules, slots, compositions against the real generated vocabulary, the share codec + its tamper battery, SVG escaping, the one-application-point vetting invariant, group 8 the OPERATOR PATH (`portal/lib/builder.mjs`: rule coverage driven by the shipped config, rule 3 proven by changing each of the other eight answers and asserting the drafted question is byte-identical, every guard called directly, the stepEvent whitelist, the run lock), and group 9 the ORIGIN predicate (`portal/lib/origin.mjs`: both loopback origins accepted, an absent Origin allowed, 16 near-misses refused, the port a parameter — the PREDICATE only; the guard's wiring can't be reached from CI), and group 10 the BUILD ANALYTICS contract (`system/analytics.mjs`: both `/build` virtual paths static, no `?b=` payload in either, each fires once, and the real URL restored verbatim across two OVERLAPPING flips in both orderings), and group 11 the REPLAY PROJECTION + THE RECORDER'S FENCE (`agent-layer/gen-replay.mjs`'s pure `projectTrace` over synthetic in-memory rows: the happy path plus the corrupted-label MUTATION that decides whether the reproduce check is real rather than a tamper check, five refusals each naming its `seq`, `--validate` and failed calls proven not to be ops, `atMs` proven to be real pacing rather than an index, the honest label, and the `KEEP_WHOLE` coupling proven by RUNNING `curateTrace` over a >700-char command — never by grepping for the constant, since it and `truncateInput` are both module-private; plus case 8, which drives BOTH callers of the shared grammar — `projectTrace` and `portal/record-build.mjs`'s exported `makeFence` — over a decoy script path (`/tmp/evil/tooling/board-op.mjs`) and asserts that both forms a real run legitimately types stay accepted, relative and absolute, because tightening the identity inside `parseOpCommand` instead would silently kill `--dry`. Importing the recorder here is safe ONLY because its SDK import is lazy, so this group rides on group 8's SDK-free invariant too; the fence's WIRING — that this predicate is what `canUseTool` receives — stays unprovable in CI, the same split group 9 lives with). Group 8 is also where the SDK-free invariant is proven, and it is proven by CI's ABSENCE of `portal/node_modules` — locally, check it with `mv portal/node_modules portal/node_modules.off && node tooling/build-checks.mjs; mv portal/node_modules.off portal/node_modules`. `build-journey` DRIVES the page across chromium + firefox + webkit (Playwright resolved out of tooling/visual-regression/node_modules, never a repo dep) — its assertions cover the share round-trip, the tamper refusals, an emptied board, a 33 MB refusal, reduced-motion, the dock mid-flow, and that all three links in resolve. Operator-run, not in CI (epic #134, tickets #137, #138, #148)
tooling/vt-verify.mjs        /build's THIRD driver, and the one the other two structurally cannot be: it asserts that the view-transition morphs actually OPEN. `build-checks` and `build-journey` only ever assert END STATES, and `system/morph.mjs` falls through to a plain mutation whenever it cannot transition — so a morph that silently stopped firing leaves every other assertion green. It wraps `document.startViewTransition` before any module evaluates and reads `document.getAnimations()` for the running `::view-transition-*` pseudos, per engine: /build's boot opens ZERO transitions (the property the pixel gate depends on — the gate never interacts, so at-rest capture can never race a morph), each of the three families opens exactly one with the group names we wrote (`bx-q-<act>` · `bb-place-<id>` · `bx-pattern` — a duplicate name would abort the whole transition silently, and shows up here as a MISSING group), a rename opens none, and reduced motion opens none while the interaction still completes. #172 extended it past /build to the five other wrapped surfaces (home + instance wizard steps, the study's tab and remove verbs, a trace step), which name nothing and so assert the count and the end state rather than group names. Load is an EXPECTED COUNT there, not an assumed zero: home opens TWO at load and they are not a morph wrapper's — `spine.mjs`:147,149 runs the hero re-skin and its revert through its own `crossfade()` (#72). The pixel gate is safe on home for a different reason than on /build: `heroBeat` sets `data-spine="ready"` only after the revert, and the gate waits on that handle. Operator-run like build-journey (epic #164, tickets #171, #172)
tooling/vt-stack-audit.mjs   the check to run BEFORE naming anything for a view transition, and the one #172 needs on every page it touches — but NOT YET as an unattended gate: hazard A false-positives on `/index` and `/roundtrip` (2 of the 7 shipped IA pages; hazard B is clean on all seven), because the noise filter discounts elements the engine reports as ANIMATING but not elements with no box at all at sample time. Fix #190 before #172 adopts it, or the red gets ignored. `view-transition-name` is inert in what an element PAINTS, not in how it STACKS: naming it makes it a stacking context AND a containing block for abs/fixed descendants. #171 shipped a real at-rest regression through exactly that — naming the breadboard places promoted them into the same paint step as the absolutely positioned SVG drawing the connection lines, which comes EARLIER in source order, so every line was covered and clipped. The pixel gate cannot catch this class of bug: `update:docker` regenerates the baseline from the same tree, so it re-baselines the regression and the next gated run passes. Two hazards per page: A · does anything MOVE when every name is removed (containing-block shift; elements with running animations are discounted by asking the engine, not by re-sampling), B · does any positioned element overlap a named one from OUTSIDE its subtree with `z-index: auto` (the #171 bug). /build carries a state matrix because its named elements only meet the line overlay in states the gate never captures — connect mode, a derived pack, an emptied board (epic #164, ticket #171)
tooling/proto-journey.mjs    the two proto pages' cross-engine driver, and the one the pixel gate structurally cannot be: that gate never INTERACTS, so it cannot tell a live control from a dead one. Asserts what #176's whole claim rests on — the SAME command from three sources (pointer click · Space on a focused radio · a `source:"agent"` action injected through `bus-toggles.mjs`'s exported `getSlotBus()` seam, never a `window.__` global) producing the SAME resulting DOM class on the SAME tile, with a reset between. Phrased as resulting DOM and never "an action was emitted", which would pass with no consumer at all. Also: `TONES` against the live vocabulary enum, reset against the FETCHED committed proposal, the two refusals (DOM untouched + named in the readout + NOTHING on the console), per-slot isolation, the frame's clamps under drag and keys, `--frame-w` unset at mount, the `@container` reflow as a track COUNT (the pixel string differs per engine), reduced motion, and that work.html's embeds grew no chrome — that last one scrolls the `loading="lazy"` iframes in and waits on the FRAME LIST, because an un-navigated lazy iframe already reports readyState "complete" and the check was vacuously true on firefox + webkit until it did. It caught a real firefox drag bug (see device-frame.mjs). Operator-run like build-journey and vt-verify, not in CI: `node tooling/visual-regression/serve.mjs &` then `node tooling/proto-journey.mjs [engine|all]` (epic #164, ticket #176)
tooling/fieldwork-kpis.mjs   ground-truth dispatch KPIs from the Fieldwork fixtures — a post-hoc JUDGE for verifying a composition run's numbers; NEVER fed to an agent prompt (epic #1 ticket #13)
```
The kb (`_factory/kb/` in the jobs folder) is the database — record shapes + parsers → `.claude/references/kb-format.md`.

## Where new code goes
- **Portal API endpoint** → a route branch in `portal/server.mjs` (`readBody` → delegate → `json(res, …)`); logic in a `portal/lib/<concern>.mjs` module, one concern per module.
- **Portal UI feature** → `portal/public/portal.js`: a hash route + render function using the existing `api()` helper; styles in `portal.css`.
- **Machine-layer artifact** → `agent-layer/gen-<output>.mjs` exporting `gen<Name>(ledger)`; register in `build.mjs` (import + call + `✓` log line), keep the standalone-run guard. Shared parsing belongs in `lib.mjs`.
- **Component** → token-only CSS in `system/components.css`; a new semantic token gets added to `system/tokens.source.json` (contract group) first, then regenerate: `node agent-layer/gen-token-css.mjs`.
- **New component spec** → `system/specs/<component>.md` (+ `.contract.json` if data-bound) per `.claude/references/kb-format.md`, then regenerate the pack: `node agent-layer/gen-handoff.mjs`.
- **New /build pattern** → a rule in `system/pattern-rules.mjs` (the rule NAMES the pattern from the board and the slots are COUNTED from it, never invented) + its entry in `PATTERNS`. Spec-first, same as any component: it may only compose components that already exist in `system/specs/` and validate against the generated `handoff/verdant/vocabulary.json` — a pattern with no components renders the honest out-of-library refusal card in `system/pattern-render.mjs`, which is the correct outcome, not a gap to fill with markup. All five current patterns are in the library (#139), so that card is unexercised — it is the sixth pattern's contract, and `build-checks` group 1 keeps a deliberately vacuous `inLibrary: false ⇒ needs` clause guarding it. Add a `BOARD_FOR` fixture in `tooling/build-checks.mjs` too: every group iterates `PATTERNS`, so a new entry with no board fails loudly rather than being silently skipped. Then `node tooling/build-checks.mjs` (groups 1–3 and 6 cover it) and `node tooling/build-journey.mjs all`.
- **WC wrapper** → `system/wc/<tag>.mjs`, spec-first (a wrapper exists only for a `system/specs/` component; shadow CSS uses only spec-head tokens, no literals, no var() fallbacks), copied into the pack by `gen-handoff`.
- **Brand/company skin** → clone `system/tokens.neutral.css` → `tokens.<company>.css` and `client.neutral.config.js` → `client.<company>.config.js`; never fork components.
- **Pack imported from Figma** → `node tooling/figma/figma-pull.mjs --slug <slug> --accent <hue> [--page Color]` → `system/tokens.<slug>.css` (`--offline` re-runs off the cache for free; `--from <export.json>` reads a plugin export instead of the API — no token, no quota, no Enterprise gate). It targets a PACK, never the contract — a Figma file holds brand values, and the contract layers stay generated from `tokens.source.json`. Same honesty rule as traces: the pack header states whose design work it is, which ramps were mapped, and every contrast negotiation and remaining WCAG failure.
- **View-time behaviour on shipped pages** → a hand-written ES module beside `system/site.js`.
- **New live-manipulable control on a shipped page** → add its `system/param-manifest.json` entry in the same PR + regenerate `node agent-layer/gen-param-count.mjs` (CI `verify` drift-checks it; an omitted control is a review-catchable gap).
- **kb record type or field** → `.claude/references/kb-format.md` (both parsers must stay in sync).
- **New scenario** → clone a `scenarios/<slug>/` package per `scenarios/README.md` + one `scenarios/index.json` entry + its imports in `worker/fixtures.mjs`; the Worker's routes (`worker/api.mjs`) never change.
- **New trace** → record a REAL run: `node portal/record-trace.mjs` → curate `node tooling/curate-trace.mjs <raw> <out>` → validate `node tooling/validate-trace.mjs`. Hand-writing or hand-editing trace content is forbidden (honesty contract, hard) — a bad run is fixed by a tighter agent prompt + re-run, never an edit.
- **New replay run** → a REAL run, the trace rule extended: write the brief by hand (`replay/briefs/<slug>.md` — the problem, never the answer, and no board), prove the mechanism with `node portal/record-build.mjs --slug <slug> --dry`, record it with `node portal/record-build.mjs --slug <slug>` (`--force` to re-run a slug — it clears the stale board AND the stale artifact first), then project it with `node agent-layer/gen-replay.mjs`. The slug names four files and `traces/` is a FLAT namespace, so it must be globally unique. **Never hand-write or hand-edit a board, a trace or an op** — a weak run is fixed by tightening `PIV_BUILD_SYSTEM`/`buildTask` in `portal/record-build.mjs` and re-running. A new op verb is a `system/board-ops.mjs` edit (the `OPS` list and its `PARAMS` entry and the switch, together) plus a build-checks group 11 case, never a special case in the generator.
- **New composition proposal** → a REAL run. **UI-first path (preferred):** the portal's "Compose a view" drawer — answer /build's ten method questions, and `portal/lib/builder.mjs`'s three committed rules draft the question from two of them (`shape`, `action`); the drafted question is EDITABLE before the run, the slot's bounds and the ethics verdict show beside it, and the PIV phases stream live (`/api/build/{config,draft,run}`). Leave the `--dry` box checked for the first run: a dry run is a full agent run over the real fixtures that writes nothing, so `in-process validateComposition ✓` is what proves the question is answerable before a real one is spent. **The equivalent CLI:** `node portal/record-composition.mjs <scenario> "<question>" <slot> [--slug <slug>]` (`--dry` first to prove auth). The scenario must carry a `scenarios/<scenario>/compose.json` (slots come from it — Fieldwork's are `summary-strip|insight-panel`). It writes `proto/compositions/<scenario>/<slug>.json` + a validating PIV trace pair and upserts the scenario-scoped `proto/compositions/<scenario>/index.json` (Fieldwork's legacy root artifacts stay at `proto/compositions/` and are only read); verify the numbers against the fixture (Fieldwork has `node tooling/fieldwork-kpis.mjs`). Same honesty rule as traces — never hand-write a composition or hand-feed an example; the `compose.json` computeRules carries DEFINITIONS ONLY (never names which tiles answer a question); a weak/invalid run is fixed by tightening the runner's committed prompt or the scenario's computeRules + re-run.
- **Platform capability (epic work)** → check `docs/epics/ai-first-ux-factory.architecture.md` first — most "new" pieces are already-decided Missing pieces with format and placement pinned.

## Ground rules (conventions)
- **Shipped pages are vanilla — hard constraint.** No framework, no bundler, no build step, no runtime deps, no live LLM calls at view time (agents run at build time; readers replay). Factory tooling is unrestricted but stays zero-dep Node ESM where possible — the portal's sole dependency is `@anthropic-ai/claude-agent-sdk`.
- **Token discipline:** components reference semantic tokens only — a brand value or literal in `components.css` is a bug. Mechanic + how to add a token → `.claude/references/token-system.md`.
- **Types:** plain JavaScript — no TypeScript, no schema library. Validate by hand at the boundary and throw (`portal/lib/intake.mjs`).
- **Errors:** throw plain `Error`s whose message names the offending path (`agent-layer/lib.mjs`); one catch-all at the server boundary returns `{ error }` JSON (`portal/server.mjs`). No error taxonomy, no wrapping.
- **Feature/entry-point files open with a header citing their governing doc** (`strategy §13`, `RUNBOOK P11`, `_factory/agent-layer.md §4`); helper modules get a plain what/why header. New files follow suit.
- **Secrets:** only in `portal/.env`, hand-parsed by `portal/lib/env.mjs` (no dotenv) — never committed, never client-side.
- **Honesty contract (hard, from the PRD):** fictional scenarios visibly labeled; traces labeled "real run, curated"; capability indicators state exactly what runs vs. what's plan-gated. Never hand-write anything presented as agent output — re-run with tighter prompts instead.
- **Git:** work on `main`; one atomic commit per phase/ticket, message = what + doc reference (`portal V1: … (strategy §13, P11)`). **A PR body MUST carry a `Closes #N` trailer** for the ticket it finishes — a title that merely mentions `(#N)` does not close anything, so merged tickets sit open and look unplanned (#78 shipped in PR #93 and stayed open for a day, costing a wasted planning pass). **A ticket's plan, report and review belong in the same PR** (`.claude/plans/`, `.claude/reports/`, `.claude/code-reviews/pr-<N>-review.md`) — four of PRs #97–#100's artifacts were written but left uncommitted in their worktrees, one `git worktree remove` away from being lost. **Deploy = commit the artifacts:** generators run at authoring time, outputs are committed, Pages serves the repo as-is — the repo itself is inspectable proof for technical readers, so never hide generated outputs behind a build step or `.gitignore`.
- **Testing:** no suite, no linter, no type-check — don't hunt for or invent one. "Done" = run the surface you touched: portal boots and `/api/health` answers; a generator prints its `✓` line; a page renders under the neutral pack.

## Working principles
- Platform work is governed by `docs/epics/ai-first-ux-factory.prd.md` + `.architecture.md` — if a change would contradict a decision recorded there, stop and flag it; don't silently drift.

## Commands
- portal: `cd portal && npm install && npm start` → http://localhost:4747
- static shell preview: `npx serve .` (repo root)
- agent layer (from the jobs folder): `node ../ux-factory/agent-layer/build.mjs _factory/kb/decisions/<company>.md`
- per-company instance (from the jobs folder; **--out MUST be outside the repo**): `node ../ux-factory/agent-layer/build-instance.mjs <brief.md> --out <dir> --pack <tokens.<slug>.css> --trace <derivation.jsonl> [--compositions <dir>] [--proto <url>] [--handoff <url>]` — builds the deploy dir + prints the deploy command (`--compositions` ships a bespoke prototype in-slot; its index.json proposal paths must already read `/proto/compositions/<slug>/…`, i.e. the record-composition run used `scenario == slug`); then run `npx wrangler pages project create inst-<slug>-<rand> --production-branch main` and `npx wrangler pages deploy <dir> --project-name inst-<slug>-<rand> --branch main` (unlisted + noindex; deploy is the operator's explicit step)
- deploy: `npx wrangler pages deploy . --project-name factory-ux --branch main`

## On-demand context
- API route work → `.claude/references/backend-api-best-practices.md` · UI work → `.claude/references/frontend-component-best-practices.md`
